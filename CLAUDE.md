# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库性质

嵌入式 Linux 课程资料库（华清远见 FS-MP1A / STM32MP157A 开发板，共 10 章）。不是代码项目，没有构建/测试流程。核心资产是 `PPT/*.md`（由课件 PPT 转换的图文完整版）及 `PPT/images/chap01~10/` 配图。

- `PPT/*.md` + `PPT/images/` 是**git 跟踪的唯一内容**，也是知识的权威载体（描述行中逐字转录了截图里的 menuconfig 路径、配置项、命令行等）。
- 原始课件 `.dps`/`.pptx` 保留在磁盘上但被 git 忽略。若 md 描述存疑，以原 PPT 截图为准。
- `01_移植U-Boot/` 存放博客原稿（实验导论 + 各实验指导，工作流和环境见下方「实验部分」）——章节目录按 PPT 大纲命名、带阅读顺序序号；大体积安装包（`*.zip`/`*.tar.xz`）及解压出的 SDK 目录（`en.SDK-*/`）被 git 忽略，随对应实验材料一起放在章节目录里（如 `01_移植U-Boot/` 内的 SDK 安装包与解压目录），不入库。

## md 文件结构与约定

- 每页幻灯片一个 section，以 `<!-- Slide number: N -->` 分隔，N 与原 PPT 页码对应，勿改动。
- 每个图片引用 `![chapNN/slideXXX_KK.ext](images/chapNN/slideXXX_KK.ext)` 的**紧下一行**必须是 `> 图：` 描述行——这是全仓库的固定格式，编辑时保持二者相邻。
- 图片按内容 md5 去重：文件名中 `slideXXX` 是该图**首次出现**的页码，`KK` 是全章出现顺序序号（不是页内序号）。同一文件可被多页引用。
- 编码 UTF-8（无 BOM）、LF 行尾。控制台里中文显示乱码通常是 GBK 显示问题，文件本身没问题——不要因此"修复"文件编码。

## PPT → Markdown 转换管线（新增章节时使用）

`.dps` 是 WPS 扩展名，但文件实际是改名的 MS PowerPoint 97-2003 (.ppt)（OLE2 复合文档，含 `PowerPoint Document` 流），PowerPoint 无法直接按 .dps 扩展名打开：

1. 复制为 `.ppt` 扩展名：`cp "${f%.dps}.ppt"`
2. PowerShell COM 批量转 `.pptx`（`pp.Presentations.Open($f, $true, $false, $false)` + `SaveAs($out, 24)`，目录路径经环境变量传入避免中文参数乱码）
3. markitdown 转 md，**必须加 UTF-8 环境变量**（否则 GBK 输出导致全文乱码）：
   ```bash
   PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/Users/GY/AppData/Local/Programs/Python/Python314/python -m markitdown "文件.pptx" > "文件.md"
   ```
4. 用 python-pptx 按幻灯片顺序提取图片（group 递归、placeholder 含 `.image` 的也算图片，按 md5 去重），并核对每页 md 引用数与实际图片数——**markitdown 会随机丢弃部分图片**（本次 555 页丢了 30 张），核对后按页补插引用。
5. 逐张查看图片补写 `> 图：` 描述，截图中的菜单路径/配置项/命令需逐字转录。

## docx → Markdown 转换管线（课程配套 word 资料转成附录时使用）

`markitdown` 能转 docx，但**图片会输出成截断的 `![](data:image/png;base64...)` 占位符、并且静默丢图**（本次 50 个图片位只输出 43 个）——不能按它的占位符位置对齐图片。可用做法：

1. 文本骨架用 markitdown（`PYTHONUTF8=1 PYTHONIOENCODING=utf-8 ... -m markitdown "x.docx" > x.md`），正文文字与加粗可信。
2. **图片落点以 python-docx 的按文档顺序转储为准**：逐段输出 `{样式}` + 正文 + `[IMG-n(imageM.png)]`（图片插入点），据此定"哪张图跟在哪句话后面"；样式名（a7/t10/t2/step/a9）用来还原层级。转储同样必须带 `PYTHONUTF8=1`（Python stdout 重定向默认 GBK，落盘即乱码）。
3. 图片从 `word/media/` 按 `document.xml` 里 `r:embed` 的出现顺序提取，按 md5 去重（本次 50 位 → 48 张唯一图，其中两张各被引用两次），最终文件名 `NN_短标签.png`（NN = 去重后的阅读顺序号，本次恰好等于 `imageM` 的 M）。
4. 两个已知转储伪影：dump 里命令文本**出现两遍**（Word 的 Choice/Fallback 双写），真实文本以 markitdown 单份为准；`v:imagedata` 计数为 0 说明图片位没有 VML 重复计数。
5. 原文自身的编号笔误、盘符差异（如本篇两个"步骤6"、"Y 盘" vs 我们的 Z: 盘）**照原文转录 + 在开头「转录说明」里注明**，不要静默"改正"原文；自己补的内容单独成节并标明"非原文内容"。

## 校验

改动 md 后应确认：旧格式死链（`![图片 x](图片x.jpg)`）为 0、每条引用下一行是 `> 图：` 行、引用文件均存在、`<!-- Slide number:` 数量不变。

## 实验部分（01_移植U-Boot/）

FS-MP1A（STM32MP157A）实验课：在 VMware 的 Ubuntu 20.04 虚拟机里做交叉编译，把 U-Boot/内核烧到开发板。当前实验对应课件《第3章 移植U-Boot》，按 Slide 范围拆成多个实验。

### 实验工作流

- 每个实验一份指导文件 `01_移植U-Boot/NN_实验N_内容.md`（博客命名规则见下），范围对应课件的具体 Slide；开头有"课件 ↔ 步骤"对应表，正文为：实验目的 → 实验环境 → 实验步骤 → 注意事项 → 完成标志 → 后续实验预告。
- **文档开头标注课件 Slide 范围（用户指定，2026-09-18）**：每篇实验文档标题下「系列说明」引用块的**首行**固定写 `> **对应课件**：《第N章 …》X.Y 节，Slide A-B`，便于对照课件原文快速定位；新文档照此格式补齐。当前映射：导论 3.1~3.3 节 Slide 1-26、实验一 3.4 节 28-31、实验二 3.4 节 32-34、实验三 3.5 节 35-42、实验四 3.5 节 43-49、实验五 3.5 节 50-59、实验六 3.5 节 60-65、实验七 3.5 节 66-68、实验八 3.5 节 69、实验九 3.5 节 70-74、实验十 3.5 节 75-81、实验十一 3.6 节 82-87（第3章共 87 页：3.1=1-5、3.2=6-14、3.3=15-26、3.4=27-34、3.5=35-81、3.6=82-87）；实验十二 4.1~4.3 节 Slide 2-16、实验十三 4.4 节 17-22、实验十四 4.5 节 23-34、实验十五 4.6 节 35-42、实验十六 4.7 节 43-50、实验十七 4.8 节 51-55（第4章《使用U-Boot》共 55 页、第 1 页为章封面：4.1=2-6、4.2=7-10、4.3=11-16、4.4=17-22、4.5=23-34、4.6=35-42、4.7=43-50、4.8=51-55）。
- **实验密度规则（用户指定，2026-09-20）**：一个可观察里程碑 = 一次烧录/验证闭环；无独立可观察效果的"无声"改动搭下一班有里程碑的车合并验证——密度对齐课件，课件在哪页安排"重新编译烧写/运行验证"，实验的验证闭环就落在哪，不为拆细而拆。第 3 章唯一密度教训：F-3+F-4 本应合并一篇（F-4 一行改动、串口零变化；课件 Slide 64→74 之间本就无烧录点，两处验证合在 F-5 那轮），已成稿的 01~12 不回改。第 4 章纯板侧命令操作、无编译烧写，一节 = 一个实验。
- 实验开始前：写指导（命令 + 预期输出 + 注意事项），各步骤留"**实际执行结果**：待补充"占位。
- 用户在虚拟机里执行，把终端输出贴回对话（原始记录由用户存到 `testN.md`），完成后把真实输出合并进指导文件：替换"待补充"、勾选完成标志——二者合二为一。
- 用户会自己往指导文件里插截图（`实验N ...assets/` 目录）并编辑文件——**每次编辑指导文件前必须重新 Read**，文件经常在对话间隙被用户更新。
- 用户偏好：指导文件只写正确流程，走弯路的尝试不写入（实验一的 SDK 误装目录已按要求删除）；但无害报错的解释（如 gc 冲突）可以保留。
- **博客文档图片规范（Typora 规则）**：博客发布的 md（01_移植U-Boot/ 各指导/导论 + 根目录专栏总导论）图片一律复制到 `./${md文件名}.assets/` 文件夹，引用写 `./${md文件名}.assets/xxx.png`——**禁止 `../PPT/images` 跨目录引用**（博客平台不解析）；PPT/*.md 仍用 `images/chapNN/` 共享图库不动。新写博客文档时先建 assets 文件夹再写引用。
- **博客文档命名规则（用户指定，2026-09-16）**：博客 md 及 assets 文件名**不含空格**，统一 `NN_短标题.md`——NN 为两位序号，按专栏全局阅读顺序递增（文件名排序即阅读顺序，方便索引）；下划线分词（避免与 U-Boot、F-1 内部连字符混淆）；assets 文件夹严格同名加 `.assets`。已定名：根目录 `00_专栏总导论_嵌入式Linux全景与学习路线.md`；01_移植U-Boot/：`01_实验导论_U-Boot基础与移植全景.md` ~ `12_实验十一_trusted版U-Boot移植.md`（01 导论、02~04 实验一~三、05 实验四、06~11 实验五~十 = F-1~F-6、12 实验十一 trusted）；02_使用U-Boot/：`13_实验十二_U-Boot命令行与环境变量操作.md` ~ `18_实验十七_其他命令与自定义启动变量.md`（14 实验十三 网络命令与TFTP服务器、15 实验十四 eMMC和SD卡操作、16 实验十五 ext4文件系统操作、17 实验十六 启动Linux内核、18 实验十七 其他命令与自定义启动变量）。后续（第4~10章实验）在新章节目录内续编。testN.md、PPT/*.md、CLAUDE.md 不适用此规则。**附录类备查文档不占全局序号（用户指定，2026-09-22）**：恢复手册/备查附录直接命名 `附录_短标题.md`、放在所属章节目录末尾（当前两篇 = `01_移植U-Boot/附录_换机换板快速恢复到实验十一结束状态.md`、`01_移植U-Boot/附录_开发环境搭建_Linux系统安装.md`），assets 命名规则同普通文档。
- **博客 assets 图片命名（用户指定，2026-09-18）**：assets 内的图片按所在文档的阅读顺序命名 `NN_短标签.ext`（NN 两位、从 01 起；短标签取自图注、不含空格），目录内按文件名排序 = 文中出现顺序；插入新图后需对后续图片重编号并同步更新正文引用。图片 alt 文本写中文短描述（同系列既有文档），不要留 Typora 原始文件名（`image-2026…`）。
- **完成标志与实测记录的呈现方式（用户指定，2026-09-22）**：各篇「实验完成标志」一律写成纯文本条目，**不用 markdown 勾选框语法**（`- [ ]` / `- [x]` 会被渲染成复选框，观感像 AI 生成）——实测过的直接写陈述句带步骤括注（"（步骤 4 实测）"），没做的写"（步骤 X，待实测）"；README 路线图同理。实测过程**优先贴命令行文本而不是截图**（博客平台压缩后截图看不清）：整段粘贴要配"该读哪几行"的引导句与逐条解读；与他篇同款的启动长日志可一句话从略并注明出处；打错/误粘连的命令行删掉不写（走弯路不入正文的规矩同样适用于串口）。
- **博客文档必须"可追溯 + 有伏笔"（用户指定，2026-09-22，实验十三起执行）**：① **素材与出处**——凡是要放进板子/服务器的文件、要装的软件、要照抄的配置，都要列出精确出处（哪个资料包 / 哪个 zip / 课件哪一节哪张 Slide / 哪份 docx 第几行）与校验值（字节数、版本号）；**自创的方案或中转目录要明写"这是我自己建的，不是课件要求的"**（例：`D:\tftpboot` 只是 Windows→Ubuntu 的中转文件夹，不是 TFTP 根目录；`tftpd64` 那次就是自创方案没标出处，让读者在资料包里找软件）。② **与后续实验的关联**——每一步都要说清"后面哪一篇会再撞上它、现在偷懒的后果是什么"：环境节末尾放一张「本篇动作 → 后面谁用 → 偷懒后果」总表，关键步骤末尾各加一句 `**关联后续实验**：…`。**正文里不要出现"伏笔"二字**（用户 2026-09-23 指定：那是内部叫法，读者看着突兀；标签要说的是"这一步跟后面哪一节有关、出问题先回看这里"）——`14_实验十三_网络操作命令与TFTP服务器搭建.md` 已于 2026-09-23 全部换完（含小标题「本篇每一步与后续实验的关联」），全仓正文已无"伏笔"字样（本文件内部仍可用该词指代这件事）。③ **避雷与保姆级**——点击路径、nano 保存键、认卡用驱动描述、改前/改后的验证命令都要写全；**环境类踩坑要写进正文**（实验十三新增第六节「踩坑实录」，按"现象 → 根因 → 怎么定位 → 怎么修"记，配现场截图），只有"自己敲错命令的一次性试错"仍不入正文。
- **仓库目录结构（用户指定，2026-09-18）**：博客文档按课件章节建仓库根级目录，命名 `NN_章节名`（NN = 阅读顺序、不含空格），章节目录内“文档 + 同名 .assets”平铺（不再按实验分子目录）；当前 `01_移植U-Boot/` 对应第 3 章 移植U-Boot，后续章节依次接 `02_使用U-Boot`、`03_移植Linux内核`……；`00_专栏总导论` 留仓库根目录。
- **博客文档不写“当前进度”**（用户指定，2026-09-18）：博客 md（专栏导论、实验导论、各实验指导）里不放「状态：已完成/进行中」「专栏目录（持续更新）」这类会随发布时间过期、需要反复同步的内容——一律写成对任何时间点的读者都成立的“写死”表述；真实的实验进度**只记录在本文件「实验进度」一节**（各实验内部的「完成标志」条目属于该篇实验的固定记录，保留；2026-09-22 用户定：一律改成纯文本条目，不用勾选框，见下）。**2026-09-19 用户进一步明确**：专栏总导论的「七、专栏目录」整节已删（用户逐篇发布，全文篇目清单必然与已发布内容对不上）；同类"全文篇目清单/发布进度段"今后一律不写。文末「→ 下一篇：《…》」指针、章节内部的实验↔Slide 路线表（如实验导论第五节，只覆盖本章且本章全部成稿）属于写死内容，保留。

### 实验环境（截至 2026-09-15，有变化以用户终端为准）

- 虚拟机：VMware + Ubuntu 20.04（focal），主机名 `cnu-virtual-machine`，用户 `cnu`，内存 4GB（编译用 `-j2` 上限）。**网络按《1. 开发环境搭建-Linux系统安装-V4.docx》配成两块网卡**：网卡 1 = **仅主机 VMnet1**（子网 `192.168.56.0/24`；Ubuntu `192.168.56.101`、Windows 侧 VMnet1 `192.168.56.10`；该文档明确要求**不填 Gateway**，以免外网流量走这块卡），网卡 2 = **NAT**（上外网/apt）；文档还要求"有桥接网卡就删除"——那是**装 Ubuntu 阶段**的规定。做板子网络实验（实验十三起）得**另加第三块网卡桥接**（VMnet0 手动指定到那块 ASIX USB 网卡），静态 `192.168.0.100` 当 `serverip`，Windows 那块 ASIX 从 `.100` 让位改 `192.168.0.99`；原有两块保持不动，所以加卡既不断网、也不掉 Z: 盘。**另注：那份文档里的"共享"是 Samba 映射网络驱动器（Win+R → `\\192.168.56.101` → 网络硬盘 Z:），它走网卡 1，动网卡 1 会掉盘；这与 VMware Tools 的 hgfs 共享文件夹（`~/Desktop/LINUX-gy`）是两回事，后者不吃网卡。**改虚拟机网络前必读的五条（2026-09-22 实测）**：① 两块 USB 网卡按**驱动描述**认，别认"以太网 N"编号——`ASIX USB to Gigabit Ethernet Family Adapter` = 接板子（`192.168.0.x`）、`Realtek USB 2.5GbE Family Controller` = 校园网上外网（DHCP `10.253.x.x`）；照编号改过一次，把 `.99` 配到了 Realtek 上，Windows 外网当场断。② 虚拟网络编辑器里 VMnet1 的两个勾「**将主机虚拟适配器连接到此网络**」+「使用本地 DHCP 服务将 IP 地址分配给虚拟机」必须常开，**且 VMnet1 的类型必须保持"仅主机模式"**——2026-09-22 那次事故的真实成因就是 VMnet1 被改成了「桥接模式」并丢了这两个勾（截图为证：编辑器列表一度只剩 `VMnet1 桥接模式 / 外部连接 ASIX` 与 `VMnet8 NAT`，**VMnet0 那一行整个不见了**）。后果：仅主机网段消失 → Windows 那块 `VMware Network Adapter VMnet1` 设备实例被删（`Get-NetAdapter -IncludeHidden`、`pnputil /enum-devices` 都查不到，但 `vmnet*.sys` 驱动文件仍在 → **不用重装 VMware**）→ `\\192.168.56.101` 网络硬盘（Z: 盘）掉线、两端互 ping 全报"目标主机不可达"，且**与 VMware 开没开机无关**。修法：编辑器里把 VMnet1 改回"仅主机模式"、勾上那两个勾、子网填回 `192.168.56.0`；若列表里 VMnet0 缺失或整体乱掉，点左下「**还原默认设置(R)**」重建三件套（会顺带把 VMnet8 子网恢复成默认，Ubuntu 的 NAT 卡是 DHCP，会自动跟上）。⑦ **加网卡只能用列表下方的「添加(A)...」**——在已有的「网络适配器」上改模式等于把网卡 1（仅主机）或网卡 2（NAT）挪走，实测差点发生（截图里第一块卡被选中改成了"桥接模式"，幸好没点确定）；加完**必须点「确定」才落盘**，验证方法：关机状态下看虚拟机目录里的 `.vmx`，应多出 `ethernet2.present = "TRUE"` + `ethernet2.connectionType = "custom"` + `ethernet2.vnet = "VMnet0"`（本机实测如此，`D:\VMware\Vmware_system_data\Ubuntu20-install\Ubuntu20-install.vmx`）。⑧ 新卡加好后 Ubuntu 里是 `ens38`（`ens33`/`ens37`/`ens38` 取自 PCI 插槽号，与 `.vmx` 的 `pciSlotNumber` 对应），状态会停在"连接中（正在获取 IP 配置）"——这条网线上没有 DHCP，等不到地址属正常，手工填静态即可；**它的连接名是中文「有线连接 1」**，用 `nmcli connection modify` 时连接名要先用 `nmcli connection show` 查，别照抄英文示例。③ 重建后 VMware 给这块适配器的是 `192.168.56.1`（该网段网关位），课程文档写 `192.168.56.10`——同网段都通，不必来回改。④ 添加/改适配器建议 `sudo poweroff` 彻底关机后做（支持热插拔，但 NetworkManager 会给新设备另建连接、改虚拟网络编辑器还会瞬断一次）；挂起不算关机。⑤ Clash 的 `Meta` 虚拟网卡占着默认路由，`Test-NetConnection <虚拟机IP> -Port 445` 会返回**假阳性** `TcpTestSucceeded=True`（ICMP 仍 False）；判共享通不通要看 `net use` 状态与实际列出 `\\192.168.56.101\share` 的内容。
- **课件截图用的是 VirtualBox**（课件里主机名 `cnu@cnu-VirtualBox`），用户实验机是 **VMware**（`cnu@cnu-virtual-machine`）——写指导文件一律按用户环境（VMware）描述，USB 转交/串口等界面操作不要照抄课件截图。
- 共享文件夹挂载点换过（实验一 `~/Desktop/share/Linux/Test1` → 现在 `~/Desktop/LINUX-gy/Test2`），新实验先看用户终端提示符确认路径。
- 工具链：`/opt/st/stm32mp1/3.1-openstlinux-5.4-dunfell-mp1-20-06-24`；激活：`source /stm32env`（软链接 2026-09-20 已确认建成，重复 `ln -s` 会报"文件已存在"，无害）或直接 `. /opt/st/stm32mp1/3.1-openstlinux-5.4-dunfell-mp1-20-06-24/environment-setup-cortexa7t2hf-neon-vfpv4-ostl-linux-gnueabi`；**各实验指导的激活命令统一写 `source /stm32env`**（2026-09-20 用户定稿，建链步骤 = 实验一步骤 6，F 系列+实验三指导已全部改写）；**每个新终端窗口都要重新激活**，`echo $CC` 验证；未激活时空敲 `$CC` 无输出，激活后空敲 `$CC` 报 `no input files` = 已生效的正常标志（课件 Slide 30 同款）。
- git 已配置（Gsheep0729 / 2697438381@qq.com）；远程 origin = `ssh://git@ssh.github.com:443/Gsheep0729/embedded-linux-stm32mp1.git`——走 443 备用端口，因为 Clash 代理会掐断 `github.com:22` 的 git 连接（报错 `Connection closed by 198.18.0.111 port 22`）；本地分支已改名 `main`。
- U-Boot 源码（已打 6 个 ST 补丁，WORKING 分支 13 条提交 = 初始 7 条 + F-1（`g2224655f`）+ F-2（`g8de188df`）+ F-3（`g1ac3a506`）+ F-4（`ec8c29dd`）+ F-5（`dd36022e`，7 文件 161+/21-）+ F-6（`88f08870`，2026-09-19 提交，2 文件 21+/0- 纯新增）；工作区干净；2026-09-19 用户曾把整树复制一份到 `Z:\Linux\Test2` 供 Windows 直读源码排查）：`~/Desktop/LINUX-gy/Test2/stm32mp1-openstlinux-5.4-dunfell-mp1-20-06-24/sources/arm-ostl-linux-gnueabi/u-boot-stm32mp-2020.01-r0/u-boot-stm32mp-2020.01`。
- basic 版编译：`make -j2 all DEVICE_TREE=stm32mp157a-fsmp1a` → 产物 `u-boot-spl.stm32`（FSBL）+ `u-boot.img`（SSBL）。
- 串口：Windows 上用 MobaXterm 的 Serial 会话连板载 ST-Link 虚拟串口，115200；`COM11` 是 2026-09-22 换电脑**之前**那台机器的实测值，**新机上的串口号待重测**（新机还要确认 ST-LINK 驱动已装、设备管理器里能看到它）。板子从 SD 卡跑我们自己移植的 U-Boot（2026-09-22 起为实验十一的 trusted 版、新板），已能停在 `STM32MP>` 命令行；F-3 之后 ADC 报错也清零（`Err: serial` 直达 `Net:`），网卡 EQOS 报错已由 F-5 消除并 ping 通 PC（2026-09-19），剩 bootm 找不到内核一类报错（内核阶段处理）。
- SD 卡烧写：读卡器经 VMware 菜单「虚拟机 → 可移动设备」连接给虚拟机后识别为 `/dev/sdb`（29.8G Storage_Device，`lsblk` 确认，以实际为准）；分区只需做一次（清场 `sgdisk -Z` + 分区 `sgdisk -g` 建 5 区 fsbl1/fsbl2/ssbl/bootfs/rootfs），之后每次改 U-Boot 只需 dd 镜像到 sdb1/sdb2/sdb3；烧写末尾加 `sudo eject /dev/sdb`（2026-09-19 用户约定：命令行弹出，免每次手动点"安全弹出"；eject 后若读卡器从虚拟机断开，下次经「虚拟机 → 可移动设备」重连）。

### 实验进度（2026-09-19）

- 已完成：实验一 Slide 28-31（安装 SDK 工具链 + 辅助工具）；实验二 Slide 32-34（解压源码 + git am 6 个 ST 补丁）；实验三 Slide 35-42（basic 版配置 + 设备树 + 首次编译）。
- 已完成（2026-09-18）：实验四 Slide 43-49（SD 卡分区烧写 + 首次启动）——指导 `01_移植U-Boot/05_实验四_SD卡分区烧写与首次启动.md` 步骤 1~8 全部回填实测与截图（gdisk 已装、SD 卡锁定 `/dev/sdb`；传承卡旧 GPT 用 `parted mklabel msdos` 擦不掉，改用 `sgdisk -Z` 真正清场（输出 `GPT data structures destroyed!`，partprobe 后 sdb1~sdb5 消失）→ `sgdisk` 重建 5 分区成功（新 GUID `69CB63CB-…`）→ 三条 dd 已写入 sdb1/sdb2/sdb3 → MobaXterm COM11 抓到 SPL 日志 + 复位循环动图）。实测串口每轮输出：`U-Boot SPL 2020.01-stm32mp-r1-g3f0216e7-dirty (Sep 15 2026 - 23:15:55 +0800)` → `Model: …STM32MP157A-DK1 Discovery Board` → `stpmic1_read: failed to read register x : 32board_init_f: probe failed clk=0 reset=0 pinctrl=0 power=-110` → `RAM: DDR3-DDR3L 16bits 533000Khz` → `stpmic1_read: failed to read register x : 39ddr power init failed` → 空行 → `resetting ...`，无限循环 = 预期"电源报错 + 复位循环"（`32`/`39` 与后句挤在一行是本版 SPL 原样输出，不是转写错）。
- 已完成（2026-09-18）：实验五 Slide 50-59（F-1 电源设备树修复）——指导 `01_移植U-Boot/06_实验五_F-1修改设备树电源配置.md` 步骤 2~10 全部回填实测与 11 张截图（六条源码自检实测、编译 + 三条 dd、新串口日志、`[WORKING 2224655f] 5 files changed, 2609 insertions(+)`）。配方：删 `&i2c4`（含 PMIC/type-C 整段）/`&cpu0`+`&cpu1`/`&usbotg_hs` 的 `port` 段/`-u-boot.dtsi` 的 `&pmic`，加 6 个 `regulator-fixed`（v3v3 / v1v8_audio / v3v3_hdmi / v1v2_hdmi / vdd / vdd_usb，`vin` 原有勿重加）、menuconfig 关 CONFIG_PMIC_STPMIC1、`cp .config configs/stm32mp15_fsmp1a_basic_defconfig` + git 提交。实测新串口：电源报错消失 → `Trying to boot from MMC1` → `MMC: no card present` → `spl: mmc init failed with error: -123` → `### ERROR ### Please RESET the board ###`（查过 `lib/hang.c`：hang() 是 `for (;;) ;` 原地死循环、不自复位，串口的两轮来自手动复位或 32s 看门狗）。
- 已完成（2026-09-18）：实验六 Slide 60-65（F-2 SD 卡 CD 引脚）——指导 `01_移植U-Boot/07_实验六_F-2修改SD卡驱动.md` 步骤 1~6 已回填实测与 4 张截图（assets 01~03 为课件原理图/课件截图，04~07 为用户实测：04 改后 nano、05 编译+三条 dd、06 串口动图、07 串口完整日志）；**步骤 7 的 git 提交待用户执行**（全文唯一一处"待补充"）。第一次执行踩坑见下方踩坑记录（`gpiob 3` 漏改 bank 名）；改正后实测串口（16:22 构建，SPL 版本串 `g2224655f-dirty`）：`Trying to boot from MMC1` 后不再报 `-123`，直接出 U-Boot 本体横幅 → `Board: stm32mp1 in basic mode (st,stm32mp157a-dk1)` → `Loading Environment from MMC... OK`（读到的是出厂系统遗留在卡数据区的有效环境，不是 bad CRC）→ `stm32 vrefbuf timed out: -110` 与 `adc@0: can't enable vdd-supply!board_check_usb_power: single shot failed for adc@0[18]!`（挤一行，F-3 对象）→ `Net: eth0: ethernet@5800a000` → 倒计时归零后 autoboot 走网络引导：`EQOS_DMA_MODE_SWR stuckFAILED: -110`×2 → `Wrong Image Format for bootm command`/`ERROR: can't get kernel image!` → 停在 `STM32MP>`。**basic 版 U-Boot 首次完整启动达成**。
- 已完成（2026-09-18）：实验七 Slide 66-68（F-3 去掉 ADC）——指导 `01_移植U-Boot/08_实验七_F-3去掉ADC功能.md` 步骤 1~6 已回填实测与 4 张截图（05 menuconfig 后自检 not set 恰两行、06 编译收尾+lsblk+三条 dd、07 串口动图、08 完整日志；assets 02/03 两张课件图内容与文件名原本对调——02 实为 menuconfig 界面、03 实为两条路径文字总览——已换正并补图注）。实测串口（17:04 构建，版本串 `g8de188df` **无 `-dirty`** = `.config` 不入库、改配置不弄脏工作区，哈希即 F-2 提交 → 实验六步骤 7 已完成）：三条 ADC 报错（含 `vrefbuf timed out`）全部消失、`Err: serial` 直达 `Net:`，EQOS×2 与 bootm 报错照旧，停在 `STM32MP>`。步骤 7（defconfig 同步 + git 提交）**已确认完成**——实验八串口版本串哈希 `g1ac3a506` 即 F-3 提交（哈希接力 `g8de188df`→`g1ac3a506`），已在指导 08 步骤 7 补记（版本串接力：实验四 `g3f0216e7` 初始 → 实验六 `g2224655f` F-1 → 实验七 `g8de188df` F-2 → 实验八 `g1ac3a506` F-3）。
- 已完成（2026-09-18）：实验八 Slide 69（F-4 关闭 LTDC）——指导 `01_移植U-Boot/09_实验八_F-4关闭LTDC显示.md` 步骤 3~6 已回填实测与 6 张截图（02 nano 保存、03 自检实测：改前 347 行 `okay`/改后 `disabled`/`ltdc_ep0_out` 计数 2、04 编译收尾+lsblk、05 三条 dd（98921/98921/853450 字节）、06 串口完整日志、07 串口动图）。配方兑现：`&ltdc` 只改 `status = "disabled"` 一行、`port` 段保留（编译通过即 dtc 检验通过）。实测串口（22:42 构建，版本串 `g1ac3a506-dirty`：哈希 = F-3 提交、`-dirty` = F-4 未提交改动）与实验七**逐行一致**——无新报错、`Err: serial` 直达 `Net:`、EQOS×2 与 bootm 照旧、停 `STM32MP>`，F-4 预期的"安静"里程碑达成。**步骤 7（git 提交）已完成（2026-09-18）**：`[WORKING ec8c29dd] 1 file changed, 1 insertion(+), 1 deletion(-)`，版本串接力至此为 g3f0216e7→g2224655f→g8de188df→g1ac3a506→ec8c29dd（指导 09 步骤 7 已回填）。assets 里 `image-20260918224833102.png` 与 04 md5 相同（重复粘贴的同一张编译+lsblk 截图），未引用备查。
- 实验九已完成（2026-09-18/19）：`01_移植U-Boot/10_实验九_F-5替换网卡驱动.md`（Slide 70-74 → 步骤 1~12；assets 01~06 = slide071_59/60、072_61、073_62/63/64，07~10 = 用户实测：menuconfig 勾选、编译收尾+三条 dd、串口完整日志、串口动图）。**步骤 1~12 全部回填实测与截图**（步骤 4~6 自检：phy.h 395 行声明、Makefile 34 行编译项、Kconfig 187~189 行三行配置项）。rar 解压会套同名子目录 `u-boot-网卡-MAE0621A驱动/`，指导步骤 2 已补"复制到共享文件夹哪一层 + 路径因人而异以终端提示符为准"说明，cp 命令按带子目录的实际路径书写。**F-5 里程碑第一半达成（2026-09-19 00:07 构建，版本串 `gec8c29dd-dirty` = F-4 提交 + F-5 未提交，接力预测兑现）**：`EQOS_DMA_MODE_SWR stuck` 消失，autoboot 走到 `Waiting for PHY auto negotiation to complete`（未插网线每秒一个点地等）；用户 Ctrl+C 中断 → `user interrupt!`/`No link`/`FAILED: 0` → bootcmd 继续两次 TFTP（遗留出厂环境 `serverip 192.168.202.6`/`ipaddr 192.168.202.12`，下载 uImage/dtb）→ `Abort`×2 → bootm 找不到内核照旧 → 停 `STM32MP>`——Ctrl+C 的回显全部正常，日志逐行解读已写入指导步骤 10。**步骤 11 已完成（2026-09-19）——F-5 里程碑全部达成**：`env default -a` 清出厂遗留 → 重新上电、倒计时内按键拦停 → 四条 setenv + `saveenv`×2（`Writing to MMC(0)` 主副本 + `Writing to redundant MMC(0)` 冗余副本）→ `ping 192.168.0.100` → `Waiting for PHY auto negotiation to complete..... done` + `host 192.168.0.100 is alive`。清环境后首次上电报 `invalid MAC address in OTP 00:00:00:00:00:00` + `Error: ethernet@5800a000 address not set.`（环境无 ethaddr 的预期报错，设完即消）；重启后 `Net:` 干净、autoboot 变默认 `Boot over mmc0!`（出厂 TFTP 引导被清掉，`** Unrecognized filesystem type **` 落回命令行）——bootcmd 不再碰网卡，PHY 长串点等待从此不出现；拦停关键一手 = 看准 `Hit any key to stop autoboot` 倒计时按 Enter（已写入指导步骤 11 板子侧开头）。assets 新增 `11_PC网卡IP设置.png`（Windows 侧 USB 网卡 ASIX"以太网 7"手动配 192.168.0.100/255.255.255.0）、`12_ping通PC.png`、`13_重启后环境生效.png`（已按规范命名并补图注；用户贴在步骤 10 结尾的 PC 配置图已挪进步骤 11 PC 侧段落）。步骤 11 注意点区已按用户要求精简：只留"遗留环境怎么清除重设（env default -a）+ 终端粘贴用右键"两条，printenv 缺失/出厂同款 MAC/不可见字符三条详细笔记撤下——`type: m` 报错最终复盘为用户手打敲错，非粘贴污染。配方：资料 rar 三件套——`phy.c`（**覆盖** `drivers/net/phy/phy.c`，厂商改造版，实测 482 行 `phy_init()` 内调用 `phy_maxio_init()`）、`maxio.c`（新增）、`dwc_eth_qos.c`（**覆盖** `drivers/net/` 下同名 MAC 驱动，即 EQOS 报错出处）；`include/phy.h` 在 `phy_realtek_init` 声明后加 `int phy_maxio_init(void);`；`drivers/net/phy/Makefile` 加 `obj-$(CONFIG_PHY_MAXIO) += maxio.o`；Kconfig 加 `config PHY_MAXIO / bool "supports the Maxio MAEXXXX PHY" / default n`（REALTEK 段附近）；menuconfig：Device Drivers → Ethernet PHY (physical media interface) support → `[*] supports the Maxio MAEXXXX PHY`。验证 = 串口不再报 `EQOS_DMA_MODE_SWR stuck`（已达成）+ `setenv ethaddr 1A:1F:DB:0E:69:FD / ipaddr 192.168.0.8 / netmask 255.255.255.0 / saveenv` 后 `ping` 通 PC（PC 网卡手动 192.168.0.X，Windows 防火墙拦 ICMP 的提醒已写入）。**步骤 12 已完成（2026-09-19）——实验九收官**：`git status --short` 恰 7 行（6 M + maxio.c ??）→ `[WORKING dd36022e] 7 files changed, 161 insertions(+), 21 deletions(-)` + `create mode 100755 drivers/net/phy/maxio.c`；版本串接力 = g3f0216e7→g2224655f→g8de188df→g1ac3a506→ec8c29dd→dd36022e，实验十构建从 `gdd36022e` 起跳；161+/21- 与 u-boot.img 仅 +600 字节互证 = 厂商改动是小幅针对性适配（maxio.c 百余行 + 覆盖文件少量修改），非整文件重写。
- 实验十已完成（2026-09-19，步骤 8 同日补完）：`01_移植U-Boot/11_实验十_F-6支持eMMC.md`（Slide 75-81 → 步骤 1~8；assets 01/02/04/07/08/11 = 课件图 slide076/077/078/079×2/080，03/05/06/09/10/12/13 = 用户实测：工具链激活+pinctrl 核对、nano 写入 sdmmc2、grep 自检、aliases/dm-spl 两处 nano、串口完整日志、串口动图——已按阅读顺序重编号并补全图注）。**F-6 里程碑达成（2026-09-19 13:58:34 构建，版本串 `gdd36022e-dirty` = F-5 提交 + F-6 未提交，接力预测兑现）**：`MMC: STM32 SD/MMC: 0, STM32 SD/MMC: 1`（实机两控制器之间是**逗号+空格**，课件截图为空格分隔，指导里里程碑串已按实机修正）→ autoboot `Boot over mmc0!` → `** Unrecognized filesystem type **` 落回 `STM32MP>`（实验九清环境后默认行为照旧）；`mmc dev 1` 选中（回显 `mmc1(part 0) is current device`，eMMC 多硬件分区后缀）+ `mmc info`：Manufacturer ID 11 / Name 004GA / MMC 5.0 / 3.7 GiB / 8-bit（`bus-width=<8>` 与 F-1 固定电源双双兑现）。编译/烧写本站无单独截图，以串口时间戳为证；`fsmp1x.dtsi` 实际落点 `^&sdmmc1` = 433 行、sdmmc2 新节点 = 446~459 行，文件里原本就有 `&sdmmc3`（本板未用，sdmmc2 插在 sdmmc1 与 sdmmc3 之间）；指导「深入一步」原预测 `mmc dev 1` 回显 `Current device is: 1` 与实机不符，已改为 `(part 0)` 版。**步骤 8 已完成（2026-09-19）——F-6 收官、实验十全部完成**：`git status --short` 恰 2 个 M → `[WORKING 88f08870] 2 files changed, 21 insertions(+)`（0 删除 = 纯新增互证）→ 工作区回干净；版本串接力定档 g3f0216e7→g2224655f→g8de188df→g1ac3a506→ec8c29dd→dd36022e→88f08870；卡上镜像仍为 `gdd36022e-dirty` 构建（与提交后内容一字不差，无需重烧）；实验十一构建从 `g88f08870` 起跳，且 trusted 流程配置改动不入库/未提交前 untracked，版本串预计**无 `-dirty`**。
- 实验十一已完成（2026-09-19，步骤 8 同日 amend 修正收官）：`01_移植U-Boot/12_实验十一_trusted版U-Boot移植.md`（Slide 82-87 → 步骤 1~8；assets 01~08：01 = 14:48 menuconfig 找到 STPMIC1（高亮仍在 `[*]`，其下紧邻一条 PALMAS 项易按错行，图注已提醒）、02 = 14:50 三站四处 grep 自检、03/06 = 课件图 slide084/086（原 01/02 按阅读顺序改号）、04 = 14:52 编译收尾 `MKIMAGE u-boot.stm32` + ls（855605 字节）、05 = 14:55 TF-A 拷入 ls（241984 字节）、07 = 15:00 MobaXterm 完整日志、08 = 串口动图）。配方：`cp .config configs/stm32mp15_fsmp1a_basic_defconfig` → `make distclean` → `cp configs/stm32mp15_trusted_defconfig configs/stm32mp15_fsmp1a_trusted_defconfig`（一条命令）→ `make stm32mp15_fsmp1a_trusted_defconfig` → menuconfig 重做三站四处（F-1 关 PMIC_STPMIC1 / F-3 关 CMD_ADC+ADC / F-5 开 PHY_MAXIO；F-2/F-4/F-6 设备树天然继承）→ `cp .config configs/stm32mp15_fsmp1a_trusted_defconfig` → `make -j2 all DEVICE_TREE=stm32mp157a-fsmp1a` 产 `u-boot.stm32`（trusted 无 SPL）→ 出厂 `tf-a-stm32mp157a-fsmp1a-trusted.stm32` dd 到 sdb1+sdb2、`u-boot.stm32` dd 到 sdb3。**trusted 版里程碑达成（2026-09-19 14:51:04 构建，版本串 `g88f08870-dirty`）**：TF-A 日志开场（`NOTICE:`/`INFO:`，BL2 v2.2-r1.0 `a70053f` Built Nov 5 2020、`Model: HQYJ FS-MP1A Discovery Board` = 华清出厂 TF-A 自报 FS-MP1A，U-Boot 段仍报 DK1——出厂 TF-A + 自移植 U-Boot 各说各的）→ `Boot used partition fsbl1` → `STM32 Image size : 855349`（dd 855605 − 256 = STM32 镜像头，数字互证加载的正是自编 U-Boot）→ `Cannot find st,stpmic1 node in DT`（FS-MP1A 本就无 STPMIC1、F-1 同款病根，TF-A 侧无害 INFO）→ `Board: stm32mp1 in trusted mode` + `MMC: 0, 1` 双控制器（设备树继承兑现）→ `Net: eth0` → `Boot over mmc0!` 落回 `STM32MP>`（basic 阶段 saveenv 的环境对 trusted 照常生效）。**预测翻车一处：版本串带 `-dirty`（曾预测无）——已破案（步骤 8 实测）**：tracked 全干净、仅两个 `??` 未跟踪文件 → 本版 U-Boot 的 dirty 判定把**未跟踪文件也算数**（对照 F-3 仅 ignored 的 `.config` 变动时无 `-dirty`）；提交并 ignore 后下次构建即干净哈希。三站四处自检实测全中（STPMIC1 not set / ADC 恰两行 / PHY_MAXIO=y）。烧写收尾按用户要求新增 `sudo eject /dev/sdb`。**步骤 8 已完成（2026-09-19 amend 修正）**：第一轮 `git add -A` 误把 TF-A 二进制提交进库（`9b7c834b`，CRLF 警告 = 库内副本已被规范化改坏、工作区原样），已按 `git rm --cached` + `.gitignore` + `git commit --amend --no-edit` 就地修复 → **新哈希 `e97b6818`**（`trusted: 新增 FS-MP1A trusted 版 defconfig`）：`git show --stat HEAD` 恰 2 文件（defconfig 1616 行 + .gitignore 1 行 = 1617 insertions，create mode 仅 defconfig）、`git status --short` 无输出、工作区回干净。文档步骤 8 实际执行结果与完成标志已回填；版本串接力定档 g3f0216e7→g2224655f→g8de188df→g1ac3a506→ec8c29dd→dd36022e→88f08870→**e97b6818**，`??` 清零后下次构建版本串预计 `ge97b6818` 不带 `-dirty`。assets 01~08 磁盘重命名已执行（2026-09-19，目录内恰好 01~08 八个文件，md 8 条引用全部命中、死链清零）。
- 实验十二已写指导（2026-09-19，未实测）：`02_使用U-Boot/13_实验十二_U-Boot命令行与环境变量操作.md`（第4章 Slide 2-16 → 步骤 1~7；assets 01~10 = 课件图 slide002_01/003_02/004_03/008_06/009_07/010_08/012_09/013_10/013_11/014_12，实测截图与回填待用户执行）。本篇不动源码、不编译、不烧写，纯板子侧串口操作；设计主线 = help 帮助系统 + 查询三件套（bdinfo/printenv/version）+ setenv 增删改（bootdelay 5、author、testvar）+ saveenv 对照实验（testvar 不保存重启即失 vs author/bootdelay 存盘仍在）。写指导时的环境假设：trusted 版命令集完整（printenv 应可用，basic 版才要 env print）；卡上版本串仍为 `g88f08870-dirty`（amend 后未重编未重烧，version 命令可反查）；板上环境基线 = 实验九的 ethaddr/ipaddr 192.168.0.8/netmask。课件演示板与我们的差异（1GB/eMMC/MMC(1)/-atk vs 512MB/SD/MMC(0)/DK1）已在指导注意事项交代"对结构不对数值"。
- 实验十三~十七指导已写（2026-09-20，未实测）：`02_使用U-Boot/` 14~18 五篇覆盖第 4 章 4.4~4.8 节（Slide 17-55），密度按"一节 = 一个实验"对齐课件（密度规则见「实验工作流」）。**实验十三**（14_网络操作命令与TFTP服务器搭建）：ping 复核 → setenv serverip 192.168.0.100 + saveenv → 服务器侧搭 TFTP（2026-09-22 改定：Ubuntu `tftpd-hpa` + VMnet0 手动桥接到 USB 网卡 + 虚拟机静态 192.168.0.100，Windows 网卡让位改 .99；原 tftpd64 方案作废）→ `tftp c2000000 uImage` 实测（预期 Bytes transferred = 7546640 = 0x732710）→ dhcp 直连拓扑预期广播超时（认知）→ nfs 不搭建只认知。**实验十四**（15_eMMC和SD卡操作命令）：mmc info/list/dev/part/read 动手 + write/erase/hwpartition 只认不动（write-once 红线）；核心任务 = `mmc dev 1` + `mmc part` 给 eMMC 摸底（出厂分区状态未知，指导带"有出厂系统/空盘"双分支，分区表截图是后续两篇的地图）；mmc read 回显十进制彩蛋（0x400=1024）。**实验十五**（16_ext4文件系统操作命令）：ext4ls/ext4load/ext4write 三条全过手，tftp(c0000000) + ext4write /test_uImage 0x732710 全链路；分区号按课件写 1:2、以实验十四实测替换；U-Boot 无 ext4 删除命令（test_uImage 留存无害）；选做步骤 6 把 dtb（0x1187d = 71805）也写进分区为实验十七铺路。**实验十六**（17_启动Linux内核命令）：tftp 内核(c2000000)+dtb(c4000000) → `bootm c2000000 - c4000000` 点火出厂 Linux（预期止于根文件系统挂载失败 = 第 5 章入口；32s 看门狗可能自动复位，指导已注明）→ setenv bootcmd 三连固化 + saveenv（此后 autoboot 归零 = 网络启动、PC 必须在线；还原法 = setenv bootcmd 清空 + saveenv）；boot 未使能时 run bootcmd 等效（课件 Slide 48 明言 ST 版未使能 boot）。**实验十七**（18_其他命令与自定义启动变量）：reset + run 主角（mybootnet/mybootemmc 两条自定义启动变量，mybootemmc 断网独立验证）+ go 只认格式，第 4 章收官命令地图。材料事实：`官方系统内核和设备树.zip` 内含且仅含 uImage（7,546,640 字节 = 0x732710）+ stm32mp157a-fsmp1a-mipi050.dtb（71,805 字节 = 0x1187d），解压套同名目录。
- 2026-09-22（**换电脑 + 换开发板**，已恢复到可开跑实验十二的形态）：新机上重新分区并烧了实验十一的 trusted 三件套。卡上镜像与旧机那次构建逐字相同（`g88f08870-dirty (Sep 19 2026 - 14:51:04)`）= 用户拷的是二进制、没有重编（重编后版本串应为 `ge97b6818`、不带 `-dirty`）；串口 `in trusted mode` + `MMC: 0, 1` + `Net: eth0` 无 EQOS 报错 → **F-1~F-6 与 trusted 配置换板全数继承，不存在"再烧一遍 F-5 驱动"这回事**。唯一复发项 = 出厂遗留环境（环境块不在 sdb1/sdb2/sdb3 三条 dd 覆盖范围内，故换板/换卡会带回 `serverip 192.168.202.6`、`ipaddr 192.168.202.100`、bootcmd 指向 TFTP、`bootdelay 0`，表现是开机直奔 TFTP 再报一串 `No link`/`ARP Retry count exceeded`/`Wrong Image Format`）→ 复原配方 = `env default -a` + 四条 `setenv` + `saveenv`×2。产出 `01_移植U-Boot/附录_换机换板快速恢复到实验十一结束状态.md`（场景决策表 + 四条判据 + 分区/编译烧写/清环境/验收五步 + 换机换板三件事）；并按用户要求在实验九/十一/十二/十三四篇各插一条 `setenv bootdelay 5` + `saveenv` 的拦停提示（板上 bootdelay 一直是 0，拦停全靠前抢那一下）。**待补**：新机 COM 号、新板 eMMC 摸底（留到实验十四）。（`saveenv` 的 `MMC(n)` 已定案 = `MMC(0)`，见下条实验十二。）
- 实验十二已实测完成（2026-09-22，新机 + 新板）：`02_使用U-Boot/13_实验十二_U-Boot命令行与环境变量操作.md` 步骤 1~7 实测全部回填（help 本版共 120 条命令、`setenv`/`printenv`/`saveenv`/`env` 全在；`bdinfo` DRAM `-> size = 0x20000000` 与 TF-A 互证；`print` 整套环境 71 条 / 5610 字节；`version` 反查卡上镜像 = `g88f08870-dirty (Sep 19 2026 - 14:51:04)`；`author` 增删与 `testvar` 不存盘对照全部兑现）。**三条新事实入档**：① `saveenv` 打印 `Writing to MMC(0)` / `Writing to redundant MMC(0)`——环境在 SD 卡，跟卡不跟板；② `serverip` 出厂默认 `192.168.1.1`，**不是"未设"**，实验十三必须先 `setenv serverip 192.168.0.100` + `saveenv` 再用 tftp，否则会静默去敲错误地址干等超时；③ **复制下来的日志里 `Hit any key to stop autoboot:  0` 恒显示 0**——U-Boot 用退格原地改写倒计时数字，终端缓冲区收尾只留 `0`，判 `bootdelay` 只能认 `print bootdelay`，别再拿这行当"bootdelay=0"的证据（本次据此修正了步骤 1/步骤 4 的措辞与排查表）。本块新板 `serial#=002E003A3131510836373834`（OTP 唯一号，认板用）。步骤 1 的实测动图 `QQ20260922-184653-HD.gif` 已补 `> 图：` 描述行，**文件名尚未按 assets 规范改号**（它在阅读顺序上应占 02，需把现 02~10 顺移）。
- 实验十三**已实测收官**（2026-09-22 晚改道 Ubuntu tftpd-hpa，2026-09-23 板子侧跑通）：`02_使用U-Boot/14_实验十三_网络操作命令与TFTP服务器搭建.md` 已按用户"方案优先对齐课件"的要求整篇改道——**TFTP 服务器从 Windows tftpd64 换成课件 5.5 那套 Ubuntu `tftpd-hpa`**（提前做完 5.5，第 5 章那节只剩复验），拓扑 = 板子 `.8` ↔ ASIX USB 网卡（Windows 让位改 `.99`）↔ VMnet0 桥接 ↔ Ubuntu `ens38` 静态 `.100` = `serverip`；实测已回填：`serverip` 设好并 saveenv、apt 装上 `tftpd-hpa`/`tftp-hpa`（5.2+20150808-1ubuntu4）、三网卡到位（`ens33` 仅主机 `.101` / `ens37` NAT `.164.128` / `ens38` `.100`）、`ping 192.168.0.99` 通（`ttl=128`、0.2 ms）、`ip route` 默认路由仍在 `ens37`、`service tftpd-hpa status` = `active (running)` 且进程命令行带 `--address :69 -l -c -s /home/cnu/tftpboot`。新增第六节「**踩坑实录**」**八条**（VMnet1 被改成桥接 → Z: 盘掉线；改现有网卡而不是「添加」；没点确定 → `.vmx` 里没有 `ethernet2`；`.99` 配到 Realtek → 外网断；`Test-NetConnection -Port 445` 假阳性；`ens38` 卡在获取 IP 属正常；**坑 7：共享目录 `cp` 过来的 `uImage` 是 `-rwx------`(700)，tftpd 以 `--user tftp` 换用户读 → `Error code 0: Permission denied`，`chmod 644` 解决，且失败会留下 0 字节的 `/tmp/uImage` 空壳造成误判**；**坑 8：网线插好板子网口灯不亮，要先在板子上 `ping` 一次驱动 PHY 自协商把链路拉起来，之后 Ubuntu 侧才 ping 得通——"互相 ping 不通"先看灯、先在板子上 ping，别急着查 IP/防火墙**），并把 `13_`~`17_` 五张实测图按 assets 规范编号入文（当时 17 张，最终计数见本条末尾）；三张重复图（`195512518`、`195724270`、`195734359`）留目录未引用备查。**测试文件传递路径已按用户要求改写（不再提 Windows 侧中转目录）**：资料包 `官方系统内核和设备树.zip`（里面仅 `uImage` 7,546,640 + `stm32mp157a-fsmp1a-mipi050.dtb` 71,805 两个文件）→ 解压放进**共享目录**（本机 `~/Desktop/LINUX-gy/Test2/官方系统内核和设备树/`）→ `cp uImage /home/cnu/tftpboot/` → **`ls -l` 看权限列**（这一步是坑 7 的入口）。**步骤 1~6 全部实测完成（2026-09-23 收官）**：板子 `ping 192.168.0.100` 与 `ping .99` 都 `is alive`；`tftp c2000000 uImage` → **`Bytes transferred = 7546640 (732710 hex)`、`1.7 MiB/s`**（`tftp` 与 `tftpboot` 两个名字实测都可用，help 里显示的是 `tftpboot`）；`dhcp` → `BOOTP broadcast 1…17` + `Retry time exceeded`（达标）；`nfs` 照抄课件 → 四行 `## Warning: gatewayip needed but not set` + `ARP Retry count exceeded`，成因 = 目标 `192.168.1.249` 与板子 `192.168.0.8/24` **不同网段且无网关**（`tftp` 同网段所以无此警告），三条出路已写进步骤 6，第 6 章 NFS 直接复用这台 Ubuntu 服务器。**已实测通过**：⑥ `chmod 644` 后本机 `tftp 127.0.0.1` 取回 7,546,640 字节（坑 7 定案：**共享目录 `cp` 过来的文件是 700，tftpd 以 `--user tftp` 换用户读 → `Permission denied`；目录 777 不解决文件权限；失败还会留下 0 字节 `/tmp/uImage` 空壳**）、步骤 5 `dhcp` 广播 `BOOTP broadcast 1…17` → `Retry time exceeded; starting again`（达标，且反证 PHY 链路是通的）。**坑 9（新增）**：`tftp c2000000 uImage` 在 Ubuntu 里敲会得到 `Error: Temporary failure in name resolution / c2000000: unknown host`——U-Boot 的 `tftp <地址> <文件>` 与 Linux tftp 客户端的 `tftp <主机> [文件]` 同名不同义，判自己在哪一侧只看提示符（`STM32MP>` vs `cnu@...$`）。**坑 8 实拍图已入文**（`24_坑8-PHY灯亮实拍.png`：左串口日志含一次 `Unknown command 'ping'` = 粘贴污染第二次现身，右实物 = 灯亮）。**步骤 6「第二发」2026-09-23 已实测**（`nfs 0xc2000000 192.168.0.100:/home/cnu/nfsboot/uImage`）：四行网关警告**全部消失**，只剩 `Loading: T T T T T T` 一路重试（截图停在第 6 个 `T`，此时仍在重试）——**能一路打 `T` 而不是报 `ARP Retry count exceeded`，说明 ARP 已问到对方 MAC、包已发出，失败点从"路由"挪到"这台服务器上没有 NFS 服务"**；两发对照表与"`Loading: T T T T…` → 往服务器侧查 / `ARP Retry count exceeded` → 往链路网段查"这条分界已写进步骤 6 与排查表。实测图现共 25 张、编号 01~25 连续（第二发那张按阅读顺序插为 21，原 21~24 顺移为 22~25）；三张未引用的重复截图已按用户要求删除，assets 目录现在只留在用的图。
- 实验十四已实测收官（2026-09-24，新板第一次摸底）：`02_使用U-Boot/15_实验十四_eMMC和SD卡操作命令.md` 步骤 1~6 全部回填（19 张图 = 19 条图注，课件图与实测图按阅读顺序混排 01~19），步骤 7 按课件"只认格式不动手"记为"无实测记录"。**四条新事实入档**：① **新板 eMMC 分区表 = GPT 五分区** `ssbl`(2 MiB, `0x22~0x1021`) / `bootfs`(64 MiB, `0x1022~0x21021`, attrs `0x4`) / `vendorfs`(16 MiB) / `rootfs`(1.13 GiB) / `userfs`(2.41 GiB)，尾扇区 `0x75ffde`×512 ≈ 3.69 GiB 与 `mmc info` 的 `Capacity: 3.7 GiB` 吻合；**课件通篇的 `mmc 1:2` 在我们板上不用改号**（2 号就叫 `bootfs`、attrs 同为 legacy-boot 标志）。② **`mmc dev [dev] [part]` 的 `part` 是 eMMC 硬件分区访问窗口（EXT_CSD `PARTITION_CONFIG`：0 用户区 / 1、2 boot1/boot2 / 3 RPMB / 4~7 GP1~GP4），不是 GPT 分区号**——只读探针定案：`mmc dev 1 2` 后 `mmc part` 报 `GUID Partition Table Header signature is wrong: 0x0 != 0x5452415020494645`（该常量按字节倒拼 = ASCII `"EFI PART"`）+ `Invalid GPT` + `Invalid Backup GPT`，`mmc dev 1` 回 `#0` 后表原样重现；课件 Slide 29 与本篇旧稿"把 2 号分区设为当前"的措辞已按实测更正，「怎么验证」排查表新增这一行症状。③ **eMMC `mmc info` 新板复测通过**：`004GA` / `MMC version 5.0` / `3.7 GiB` / **`Bus Width: 8-bit`**（F-1 + F-6 未退化），另有 SD 卡不报的 `Erase Group Size: 512 KiB`（**SD 卡是 512 Bytes，差 1024 倍**——"eMMC 上随手一次 erase"的量化代价）、`Boot Capacity: 2 MiB ENH`、`RPMB Capacity: 512 KiB ENH`；**boot 硬件分区的存在正好解释 eMMC 的 GPT 里为什么没有 `fsbl1`/`fsbl2`**（探针切到 boot2 读到全零 = 出厂只用 boot1；boot1 里是不是 TF-A 本篇未验，留实验十七）。④ **SD 卡 `mmc part` 实测**：`fsbl1`/`fsbl2` 各 256 KiB、`ssbl` 2 MiB、`bootfs` 64 MiB（**4 号**，attrs `0x4`）、`rootfs` 29.7 GiB；`type` 的可读名 SD 卡是 `0fc63daf-…` → `linux`、eMMC 是 `ebd0a0a2-…` → `data`。**实用结论：同一个 `bootfs` 角色在 eMMC 上是 2 号、在 SD 卡上是 4 号，ext4 命令 `mmc 1:2` 与 `mmc 0:4` 别记混**。附带记录：SD 卡 `mmc info` = `SD32G` / `29.7 GiB` / `4-bit` / `SD version 3.0`；`mmc list` 的后缀打法与课件不同（我们 `0 (SD)` + `1` 无后缀，课件 `0` + `1 (eMMC)`）——**认设备只看序号 + `mmc info` 的 Name，别认后缀**。
- 当前进度小结：**第一阶段（第 3 章《移植 U-Boot》，文档 01~12）全章闭环**——实验一~十一全部实测完成、F-1~F-6 全部提交、trusted 收官提交 `e97b6818` 落定；**第二阶段（第 4 章《使用 U-Boot》）指导全量成稿**——`02_使用U-Boot/` 13~18 六篇覆盖 4.1~4.8 全部小节，实验十二~十四已实测收官（2026-09-22 / 09-23 / 09-24；十三含 nfs 两发对照，十四含 eMMC 分区表摸底）；十五~十七待实测。**实验十三的 TFTP 服务器已定档走课件路线（2026-09-22 用户指定）**：Ubuntu 虚拟机里 `tftpd-hpa`（等于提前做完课件 5.5 节），VMnet0 **手动**桥到那块 ASIX USB 网卡，虚拟机静态 `192.168.0.100` = 板子的 `serverip`，宿主机那块网卡让位改 `192.168.0.99`；虚拟机是三网卡并存（仅主机 / NAT / 桥接），所以装包与改桥接的先后顺序不必讲究——NAT 卡全程在线（早期写的"先 apt 装包后改桥接"是按单网卡说的，已作废）。原"tftpd64 + `D:\tftpboot`"方案作废——资料包里从来没有 tftpd64，课件也从不在 Windows 上搭服务器（这是自创方案，用户要求：方案优先对齐课件原文）。实验密度规则（2026-09-20 定档）：一节 = 一个实验、验证闭环对齐课件验证点，见「实验工作流」。
- 后续实验材料在 `D:\桌面文件\资料\嵌入式linux`：`tf-a-stm32mp157a-fsmp1a-trusted.stm32`（trusted 版 TF-A）、`u-boot-网卡-MAE0621A驱动.rar`（F-5 的 phy.c/maxio.c/dwc_eth_qos.c）、`u-boot电源配置-设备树.dts`（F-1 参考）、`官方系统内核和设备树.zip`、kernel 相关 zip、buildroot/busybox 等。**第 4 章（实验十四~十七）需要的材料只有 `官方系统内核和设备树.zip` 一个**（zip 里就 `uImage` 7,546,640 + `stm32mp157a-fsmp1a-mipi050.dtb` 71,805 两个文件），已按用户要求复制一份放在 `02_使用U-Boot/`（md5 `29eb6127402eff8a86b43ec4e6d30a6f`，与源文件一致；`*.zip` 被 .gitignore 忽略，不入库）；实验十四全程不碰 PC 侧文件，十五/十六才需要把这两个文件送进 `/home/cnu/tftpboot`（记得 `chmod 644`）。其余：`kernel-初始设备树.zip`/`kernel-网卡驱动.zip` 是第 5 章、`busybox-1.32.0`/`buildroot-2020.02.6` 第 6~7 章、`drivers-dev.zip` 第 9 章、`graphics.tar.gz` 第 10 章、`group-b.zip` 是上机测验题目包（`rfs-exam.tar.bz2`、`u-boot-b.stm32`），本阶段都不动。

### 实验踩坑记录（避免重复踩）

- SDK 解压目录里**没有 `install.sh`**，安装脚本是长名字的 `st-image-weston-*.sh`；安装目标目录必须直接回车用默认 `/opt/st/...`（虚拟机共享文件夹不支持 Linux 符号链接，工具链不能装在里面）。
- `git init` 必须在解压出的 `u-boot-stm32mp-2020.01` 子目录里执行；在外层 `r0` 目录执行会把补丁文件本身当源码提交，且 `../*.patch` 找不到补丁（`rm -rf .git` 后重来）。
- `cc1: error: bad value ('generic-armv7-a') for '-mtune=' switch` = 当前终端没激活工具链，make 落到了 x86 宿主机 gcc（报错列出的 -mtune 参数全是 Intel/AMD CPU 即铁证）→ `source` 工具链后续编即可，无需 clean。
- `fatal: 已经有一个 gc 正运行`、ST 补丁的空白字符警告（`new blank line at EOF`）均无害，忽略。
- 编译在共享文件夹里能跑但慢，Ctrl+C 后 `make -j2` 可安全续编（进度保留）；若报符号链接/权限类错误，把源码移到虚拟机本地磁盘（如 `~/FS-MP1A/`）再编（挪动后需 `make distclean` + 重新 defconfig）。
- `sudo parted -s /dev/sdb mklabel msdos` **无任何输出是正常的**（`-s` 脚本模式成功即静默、瞬间完成，不用等），但它**不会清掉旧 GPT**：实测复查 `sgdisk -p` 连磁盘 GUID 都不变（sgdisk 见 GPT 有效就继续按 GPT 显示）。要真清场用 `sudo sgdisk -Z /dev/sdb`（GPT、MBR 数据结构一并摧毁，官方定位就是"重新分区之前"用它），再 `sudo partprobe /dev/sdb` 刷新——`lsblk`/`/dev/sdbN` 只是内核缓存的旧分区表，不 partprobe 就永远显示旧分区。实测顺序：**先 partprobe 没用**（旧 GPT 还在，重读出来还是旧分区），必须 `-Z` 之后再 partprobe 才看到分区节点消失；`-Z` 的成功输出是 `GPT data structures destroyed!`。空盘上 `sgdisk -p` 显示 `Creating new GPT entries in memory.` + 一个随机 GUID（内存态，写盘才固定）。
- `sgdisk -g` = `--mbrtogpt`（把 MBR 标签的盘转成 GPT），不是"使用 GPT 分区表"；在 MBR/BSD 标签的盘上写入必须带它，否则 sgdisk 拒绝写盘（退出码 3）。
- **写 F 系列指导时的"标准答案"来源**：DK1 的 `stm32mp15xx-dkx.dtsi`（= 用户文件 `stm32mp15xx-fsmp1x.dtsi` 的原样拷贝，行号完全一致）可从 `D:\桌面文件\资料\嵌入式linux\en.SOURCES-*.tar.xz` 追出——外层 xz 里是 `u-boot-stm32mp-2020.01-r0.tar.gz`（内层不含 dkx.dtsi）+ 6 个补丁；dkx.dtsi 是补丁 0004（DEVICETREE）**新建**的文件，取该补丁 `new file mode` 段剥掉行首 `+` 即得全文（同理可解出任意 ST 源码文件，写后续指导前先核对实际内容再下笔）。
- **F-1 文件锚点**（`stm32mp15xx-fsmp1x.dtsi`）：`/ {` 根节点 11~98 行，末尾**已有** `vin`（91~97），6 个固定电源追加在 vin 之后；`&cpu0`/`&cpu1` 在 134~140；`&i2c4` 整段 261~443（含 `stusb1600@28` 与 `pmic: stpmic@33`）；`&usbotg_hs` 在 736~747（只删 `port { ... };` 段，phys/phy-names/usb-role-switch/status 保留）。删 &i2c4 后仍被引用的电源标签即固定电源清单的由来：vdd（&adc/&vrefbuf）、v3v3（&sdmmc1/2 的 vmmc-supply）、v1v8_audio（音频）、v3v3_hdmi/v1v2_hdmi（HDMI）、vdd_usb（&usbphyc_port0/1 的 phy-supply）。
- **F-2 一行改动**（`stm32mp15xx-fsmp1x.dtsi` 的 `&sdmmc1`）：`cd-gpios` 由 `<&gpiob 7 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>` 改为 `<&gpioh 3 ...>`（DK1 原始为 dkx.dtsi 第 575 行；F-1 增删后约在 440 行附近）。**不需要额外使能 gpioh**：板级 `stm32mp15xxac-pinctrl.dtsi`（被 fsmp1a.dts 包含）里 gpioh 已是 `status = "okay"`，`stm32mp15-u-boot.dtsi` 里又标了 `u-boot,dm-pre-reloc`（SPL 可用）。`-123` = `-ENOMEDIUM`。厂商侧交叉验证：`资料/嵌入式linux/kernel-初始设备树.zip` 里 FS-MP1A 内核 dtsi 的 `&sdmmc1` 与课件一致（同 zip 还有 `官方系统内核和设备树.zip` 的 `stm32mp157a-fsmp1a-mipi050.dtb`）。
- **F-2 踩坑实录（2026-09-18 第一次执行）**：把 `gpiob 7` 只改了数字 → `gpiob 3`，**漏改 bank 名**，编译烧写三条 dd 全部成功但串口原样报 `-123`。排查钥匙是 SPL 版本串：`g2224655f-dirty (Sep 18 2026 - 15:19:33)` 与上次（`g3f0216e7 … Sep 15`）哈希、时间戳都变了 = 卡上镜像确实换过 → 问题只剩"源码那行没改对"，回看 nano 截图立刻定位。教训：GPIO 要改的是"**组名 + 序号**"两个词；步骤里的 `grep -n "cd-gpios"` 自检不能跳。用户 assets 里留有这次失败的 3 张截图（`image-20260918151853302/152349286.png`、`QQ20260918-152446-HD.gif`，未被文档引用），按"走弯路不入正文"惯例保留在 assets 备查、不入库引用。
- **F-4 锚点与"别删 port"**（`stm32mp15xx-fsmp1x.dtsi` 的 `&ltdc`）：DK1 原始节点 = 3 行 pinctrl + `status = "okay"` + `port { ltdc_ep0_out: endpoint@0 { remote-endpoint = <&sii9022_in>; }; }`（patch 0004 核对过）；sii9022 与 cs42l51 同在 **&i2c1** 下（非 &i2c4，故 F-1 删 i2c4 未伤及，编译才一直通过），其 `sii9022_in` **反向引用** `&ltdc_ep0_out` → F-4 只能改 `status` 一行、删 `port` 必报 `Reference to non-existent node or label`。课件 slide069 截图是节点上半截（连收尾 `};` 都没拍到），"截图里没有 port"≠"要删 port"。另：`stm32mp15-u-boot.dtsi` 里有 `&ltdc { u-boot,dm-pre-proper; }` 叠加，与 status 改动不冲突。
- **basic 版 U-Boot 命令集是裁剪过的**（2026-09-19 实测；**2026-09-22 存疑待复验**——trusted 与 basic 两份配置里环境变量相关开关逐条同值，那次 `printenv` 报错很可能是下一条"不可见字符"的同因）：`printenv` 报 `Unknown command`（当时判为未编入），查变量用 `env print` 替代；课件流程只用 setenv/saveenv/ping 故不受影响。拦停 autoboot 的正确姿势 = **倒计时内按任意键（回车）**直接进 `STM32MP>`（全程不碰网络）；倒计时归零后 autoboot 会拉网卡——`Waiting for PHY auto negotiation` 逐秒打点，Ctrl+C 可掐（`user interrupt!`/`No link`/`FAILED: 0`/`Abort` 均为正常回显），但该会话已"用过网络"，ethaddr 要断电重启后再设。**write-once 与 ethaddr（2026-09-19 定论）**：出厂遗留环境带整套网络配置（ethaddr=1A:1F:DB:0E:69:FD=课件 MAC、serverip 192.168.202.6 等），ethaddr 内置 `mo` 双标志（本构建未开 ENV_OVERWRITE/OVERWRITE_ETHADDR_ONCE：m=MAC 格式校验、o=write-once 已有值不许覆盖）→ 直接重设会被拒；解法 `env default -a` 清整套遗留（ethaddr 一并清空）→ 四条重设 + saveenv（可敲两次把主/冗余两份副本都刷了）。过程中出现过的 `## Error: flags type check failure ... (type: m)` 报错最终复盘 = 用户手打敲错（重打即过），不是格式校验问题（源码 `env/flags.c` 的 `eth_validate_ethaddr_str` 用 isxdigit 判定，课件 MAC 本身合法；write-once 的拒绝报错文案是 `Can't overwrite`）。VM/MobaXterm 终端里粘贴命令用**右键**（Ctrl+C 是中断、Ctrl+V 不生效）。
- **串口报 `Unknown command 'xxx'` ≠ 命令没编进来**（2026-09-22 定案；起因 = 换板后在 trusted 版敲课件原句 `setenv ethaddr ...` 被拒，一度误记为"trusted 命令集缺 setenv"）：三条证据证明命令在位——① `help` 列表里 `setenv`/`printenv`/`saveenv`/`env` 四条全在；② **手敲** `setenv`（不带参数）正常打出用法；③ `.config`(trusted) 与 `configs/stm32mp15_fsmp1a_basic_defconfig` 的 `CONFIG_CMD_SAVEENV` / `CMD_ENV_EXISTS` / `CMD_NVEDIT_INFO` 逐条同值，且这套 2020.01 源码里**不存在 `CMD_SETENV` / `CMD_PRINTENV` 符号**，`cmd/nvedit.c:1499` 的 `U_BOOT_CMD_COMPLETE(setenv, ...)` 外面没有可关它的开关（`setenv` 与 `env set` 只是 `do_env_set` 的两个门牌）。真因 = 那一行混进了终端不显示的字符（零宽空格 U+200B、BOM U+FEFF、不换行空格 U+00A0、全角空格 U+3000）——U-Boot 取命令词只跳空格与制表符，多余字节被算进命令名，报的是"这个**名字**不存在"，而串口回显与事后复制出来的文本看着仍是干净的 `setenv`。**排查顺序固定为：手敲一遍 → `help` 查列表 → 最后才怀疑配置**；粘贴只用 MobaXterm 右键，别从 PDF/网页/Word 直接拷命令进串口。据此已把 10（实验九）/12（实验十一）/13（实验十二）/14（实验十三）四篇的 bootdelay 提示与 13 的"命令集差异提示"改为两种写法并列 + 这条排查顺序。另：清环境用的 `env default -a` 没有对应的顶层老名字，只有 `env` 子命令写法，与 `setenv` 混用不是笔误。
- **F-5/F-6/trusted 锚点（patch 0004 + 课件逐字核对过）**：F-5 的 `phy.c`/`dwc_eth_qos.c` 是**覆盖**同名文件的厂商改造版（新 phy.c 的 `phy_init()` 会调用 `phy_maxio_init()`，可 grep 验证拷贝成功），`maxio.c` 纯新增；F-5 已提交兑现（`dd36022e` = 恰 7 文件，161+/21-）。F-6 纯新增：dkx.dtsi **没有** `&sdmmc2` 板级节点（全 patch 只有一处 `+&sdmmc2` 在 ev1 系文件里；dkx 只有 pinctrl 五个定义 + `stm32mp15-u-boot.dtsi` 的 compatible 覆盖），官方参照是 `stm32mp157a-ed1-u-boot.dtsi`（EV1 板自带 eMMC：mmc1 alias + `&sdmmc2 { u-boot,dm-spl; }` + sdmmc2 两个 pinctrl 节点的 dm-spl——课件只要求控制器节点，pinctrl dm-spl 未要求）；`&sdmmc1 { u-boot,dm-spl; }` 是 DK1 原有。aliases 里 `i2c3 = &i2c4` 在 F-1 删板级 &i2c4 后仍合法：控制器本体 `i2c4: i2c@5c002000` 在 SoC 级 `stm32mp151.dtsi`（status 默认 disabled），F-1 删的只是板级扩展段。trusted 版：dk1-u-boot.dtsi（= 用户 `-u-boot.dtsi`）里 clk_hse/pmic/rcc 时钟树/sai2/sdmmc1 dm-spl 等整段包在 `#ifndef CONFIG_STM32MP1_TRUSTED`——trusted 编译自动剔除（电源时钟归 TF-A 经 SCMI 管控；`stm32mp15-u-boot.dtsi` 同样 `#ifndef TRUSTED` 才 include no-scmi），trusted 配置下无 SPL、烧写组合 = TF-A×2 + `u-boot.stm32`。课件 trusted 配置起点 = `stm32mp15_trusted_defconfig`。F-6 加的 `&sdmmc2 { u-boot,dm-spl; }` 恰落在 dk1-u-boot.dtsi 的 `#ifndef CONFIG_STM32MP1_TRUSTED` 段内（`#endif` 前紧挨 `sdmmc1_b4_pins_a`，2026-09-19 nano 截图可见）→ trusted 构建自动剔除、无害（trusted 无 SPL）；aliases 的 `mmc1 = &sdmmc2;` 在文件顶部 #ifndef 之外，trusted 保留 → 实验十一里程碑 `MMC: 0, 1` 不受影响。
- **实验十一 git 踩坑（2026-09-19）**：①`git add -A` 把步骤 5 拷进源码顶层的 `tf-a-stm32mp157a-fsmp1a-trusted.stm32`（241984 字节二进制）一并提交（`[WORKING 9b7c834b] 2 files changed, 1616 insertions(+)`），add 时报 `warning: …中的 CRLF 将被 LF 替换` = git 按文本做换行规范化、**库里副本已不是逐字节原样**（工作区文件未动）——二进制素材绝不能入库；正确流程 = 先 `echo "tf-a-..." >> .gitignore` 再 `git add -A`；已提交的用 `git rm --cached <文件>` + 补 `.gitignore` + `git commit --amend --no-edit` 修复——2026-09-19 已照此修复完毕：新哈希 `e97b6818`，1617 insertions 恰 = defconfig 1616 行 + .gitignore 1 行（互证 TF-A 已出库、库里无残留）。②**`-dirty` 之谜破案**：构建时 tracked 全干净、仅两个 `??`（defconfig + tf-a）→ 版本串仍带 `-dirty`；对照 F-3（只有被忽略的 `.config` 变动 → 无 `-dirty`）得出：**本版 U-Boot 的 dirty 判定把未跟踪文件也算数、被忽略文件不算**。③WMware 共享文件夹里的文件（如 tf-a 素材）拷进源码顶层后就是普通 untracked 文件，`add -A` 前先 `git status --short` 过一眼。
