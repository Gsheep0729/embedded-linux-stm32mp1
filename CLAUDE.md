# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库性质

嵌入式 Linux 课程资料库（华清远见 FS-MP1A / STM32MP157A 开发板，共 10 章）。不是代码项目，没有构建/测试流程。核心资产是 `PPT/*.md`（由课件 PPT 转换的图文完整版）及 `PPT/images/chap01~10/` 配图。

- `PPT/*.md` + `PPT/images/` 是**git 跟踪的唯一内容**，也是知识的权威载体（描述行中逐字转录了截图里的 menuconfig 路径、配置项、命令行等）。
- 原始课件 `.dps`/`.pptx` 保留在磁盘上但被 git 忽略。若 md 描述存疑，以原 PPT 截图为准。
- `01_移植U-Boot/` 存放博客原稿（实验导论 + 各实验指导，工作流和环境见下方「实验部分」）——章节目录按 PPT 大纲命名、带阅读顺序序号；大体积安装包（`*.zip`/`*.tar.xz`）及解压出的 SDK 目录（`en.SDK-*/`）被 git 忽略，随对应实验材料一起放在章节目录里（如 `01_移植U-Boot/` 内的 SDK 安装包与解压目录），不入库。

## md 文件结构与约定

- 每页幻灯片一个 section，以 `<!-- Slide number: N -->` 分隔，N 与原 PPT 页码对应，勿改动。
- 每个图片引用 `![chapNN/slideXXX_KK.ext](images/chapNN/slideXXX_KK.ext)` 的**紧下一行**必须是 `> 图：` 描述行——这是全仓库的固定格式，编辑时保持二者相邻。
- 图片按内容 md5 去重：文件名中 `slideXXX` 是该图**首次出现**的页码，`KK` 是全章出现顺序序号（不是页内序号）。同一文件可被多页引用。
- 编码 UTF-8（无 BOM）、LF 行尾。控制台里中文显示乱码通常是 GBK 显示问题，文件本身没问题——不要因此"修复"文件编码。

## PPT → Markdown 转换管线（新增章节时使用）

`.dps` 是 WPS 扩展名，但文件实际是改名的 MS PowerPoint 97-2003 (.ppt)（OLE2 复合文档，含 `PowerPoint Document` 流），PowerPoint 无法直接按 .dps 扩展名打开：

1. 复制为 `.ppt` 扩展名：`cp "${f%.dps}.ppt"`
2. PowerShell COM 批量转 `.pptx`（`pp.Presentations.Open($f, $true, $false, $false)` + `SaveAs($out, 24)`，目录路径经环境变量传入避免中文参数乱码）
3. markitdown 转 md，**必须加 UTF-8 环境变量**（否则 GBK 输出导致全文乱码）：
   ```bash
   PYTHONUTF8=1 PYTHONIOENCODING=utf-8 /c/Users/GY/AppData/Local/Programs/Python/Python314/python -m markitdown "文件.pptx" > "文件.md"
   ```
4. 用 python-pptx 按幻灯片顺序提取图片（group 递归、placeholder 含 `.image` 的也算图片，按 md5 去重），并核对每页 md 引用数与实际图片数——**markitdown 会随机丢弃部分图片**（本次 555 页丢了 30 张），核对后按页补插引用。
5. 逐张查看图片补写 `> 图：` 描述，截图中的菜单路径/配置项/命令需逐字转录。

## 校验

改动 md 后应确认：旧格式死链（`![图片 x](图片x.jpg)`）为 0、每条引用下一行是 `> 图：` 行、引用文件均存在、`<!-- Slide number:` 数量不变。

## 实验部分（01_移植U-Boot/）

FS-MP1A（STM32MP157A）实验课：在 VMware 的 Ubuntu 20.04 虚拟机里做交叉编译，把 U-Boot/内核烧到开发板。当前实验对应课件《第3章 移植U-Boot》，按 Slide 范围拆成多个实验。

### 实验工作流

- 每个实验一份指导文件 `01_移植U-Boot/NN_实验N_内容.md`（博客命名规则见下），范围对应课件的具体 Slide；开头有"课件 ↔ 步骤"对应表，正文为：实验目的 → 实验环境 → 实验步骤 → 注意事项 → 完成标志 → 后续实验预告。
- **文档开头标注课件 Slide 范围（用户指定，2026-09-18）**：每篇实验文档标题下「系列说明」引用块的**首行**固定写 `> **对应课件**：《第N章 …》X.Y 节，Slide A-B`，便于对照课件原文快速定位；新文档照此格式补齐。当前映射：导论 3.1~3.3 节 Slide 1-26、实验一 3.4 节 28-31、实验二 3.4 节 32-34、实验三 3.5 节 35-42、实验四 3.5 节 43-49、实验五 3.5 节 50-59、实验六 3.5 节 60-65、实验七 3.5 节 66-68、实验八 3.5 节 69、实验九 3.5 节 70-74、实验十 3.5 节 75-81、实验十一 3.6 节 82-87（第3章共 87 页：3.1=1-5、3.2=6-14、3.3=15-26、3.4=27-34、3.5=35-81、3.6=82-87）。
- 实验开始前：写指导（命令 + 预期输出 + 注意事项），各步骤留"**实际执行结果**：待补充"占位。
- 用户在虚拟机里执行，把终端输出贴回对话（原始记录由用户存到 `testN.md`），完成后把真实输出合并进指导文件：替换"待补充"、勾选完成标志——二者合二为一。
- 用户会自己往指导文件里插截图（`实验N ...assets/` 目录）并编辑文件——**每次编辑指导文件前必须重新 Read**，文件经常在对话间隙被用户更新。
- 用户偏好：指导文件只写正确流程，走弯路的尝试不写入（实验一的 SDK 误装目录已按要求删除）；但无害报错的解释（如 gc 冲突）可以保留。
- **博客文档图片规范（Typora 规则）**：博客发布的 md（01_移植U-Boot/ 各指导/导论 + 根目录专栏总导论）图片一律复制到 `./${md文件名}.assets/` 文件夹，引用写 `./${md文件名}.assets/xxx.png`——**禁止 `../PPT/images` 跨目录引用**（博客平台不解析）；PPT/*.md 仍用 `images/chapNN/` 共享图库不动。新写博客文档时先建 assets 文件夹再写引用。
- **博客文档命名规则（用户指定，2026-09-16）**：博客 md 及 assets 文件名**不含空格**，统一 `NN_短标题.md`——NN 为两位序号，按专栏全局阅读顺序递增（文件名排序即阅读顺序，方便索引）；下划线分词（避免与 U-Boot、F-1 内部连字符混淆）；assets 文件夹严格同名加 `.assets`。已定名：根目录 `00_专栏总导论_嵌入式Linux全景与学习路线.md`；01_移植U-Boot/：`01_实验导论_U-Boot基础与移植全景.md` ~ `12_实验十一_trusted版U-Boot移植.md`（01 导论、02~04 实验一~三、05 实验四、06~11 实验五~十 = F-1~F-6、12 实验十一 trusted）。后续（第4~10章实验）在新章节目录内从 13 续编。testN.md、PPT/*.md、CLAUDE.md 不适用此规则。
- **博客 assets 图片命名（用户指定，2026-09-18）**：assets 内的图片按所在文档的阅读顺序命名 `NN_短标签.ext`（NN 两位、从 01 起；短标签取自图注、不含空格），目录内按文件名排序 = 文中出现顺序；插入新图后需对后续图片重编号并同步更新正文引用。图片 alt 文本写中文短描述（同系列既有文档），不要留 Typora 原始文件名（`image-2026…`）。
- **仓库目录结构（用户指定，2026-09-18）**：博客文档按课件章节建仓库根级目录，命名 `NN_章节名`（NN = 阅读顺序、不含空格），章节目录内“文档 + 同名 .assets”平铺（不再按实验分子目录）；当前 `01_移植U-Boot/` 对应第 3 章 移植U-Boot，后续章节依次接 `02_使用U-Boot`、`03_移植Linux内核`……；`00_专栏总导论` 留仓库根目录。
- **博客文档不写“当前进度”**（用户指定，2026-09-18）：博客 md（专栏导论、实验导论、各实验指导）里不放「状态：已完成/进行中」「专栏目录（持续更新）」这类会随发布时间过期、需要反复同步的内容——一律写成对任何时间点的读者都成立的“写死”表述；真实的实验进度**只记录在本文件「实验进度」一节**（各实验内部的「完成标志」勾选表属于该篇实验的固定记录，保留）。

### 实验环境（截至 2026-09-15，有变化以用户终端为准）

- 虚拟机：VMware + Ubuntu 20.04（focal），主机名 `cnu-virtual-machine`，用户 `cnu`，内存 4GB（编译用 `-j2` 上限）。
- **课件截图用的是 VirtualBox**（课件里主机名 `cnu@cnu-VirtualBox`），用户实验机是 **VMware**（`cnu@cnu-virtual-machine`）——写指导文件一律按用户环境（VMware）描述，USB 转交/串口等界面操作不要照抄课件截图。
- 共享文件夹挂载点换过（实验一 `~/Desktop/share/Linux/Test1` → 现在 `~/Desktop/LINUX-gy/Test2`），新实验先看用户终端提示符确认路径。
- 工具链：`/opt/st/stm32mp1/3.1-openstlinux-5.4-dunfell-mp1-20-06-24`；激活：`source /stm32env`（符号链接可能未建）或直接 `. /opt/st/stm32mp1/3.1-openstlinux-5.4-dunfell-mp1-20-06-24/environment-setup-cortexa7t2hf-neon-vfpv4-ostl-linux-gnueabi`；**每个新终端窗口都要重新激活**，`echo $CC` 验证。
- git 已配置（Gsheep0729 / 2697438381@qq.com）；远程 origin = `ssh://git@ssh.github.com:443/Gsheep0729/embedded-linux-stm32mp1.git`——走 443 备用端口，因为 Clash 代理会掐断 `github.com:22` 的 git 连接（报错 `Connection closed by 198.18.0.111 port 22`）；本地分支已改名 `main`。
- U-Boot 源码（已打 6 个 ST 补丁，WORKING 分支 12 条提交 = 初始 7 条 + F-1（`g2224655f`）+ F-2（`g8de188df`）+ F-3（`g1ac3a506`）+ F-4（`ec8c29dd`）+ F-5（`dd36022e`，2026-09-19 提交，7 文件 161+/21-）；工作区干净；2026-09-19 用户曾把整树复制一份到 `Z:\Linux\Test2` 供 Windows 直读源码排查）：`~/Desktop/LINUX-gy/Test2/stm32mp1-openstlinux-5.4-dunfell-mp1-20-06-24/sources/arm-ostl-linux-gnueabi/u-boot-stm32mp-2020.01-r0/u-boot-stm32mp-2020.01`。
- basic 版编译：`make -j2 all DEVICE_TREE=stm32mp157a-fsmp1a` → 产物 `u-boot-spl.stm32`（FSBL）+ `u-boot.img`（SSBL）。
- 串口：Windows 上用 MobaXterm 的 Serial 会话连板载 ST-Link 虚拟串口（实测为 `COM11`），115200；板子从 SD 卡跑我们自己的 basic 版 U-Boot，已能停在 `STM32MP>` 命令行；F-3 之后 ADC 报错也清零（`Err: serial` 直达 `Net:`），网卡 EQOS 报错已由 F-5 消除并 ping 通 PC（2026-09-19），剩 bootm 找不到内核一类报错（内核阶段处理）。
- SD 卡烧写：读卡器经 VMware 菜单「虚拟机 → 可移动设备」连接给虚拟机后识别为 `/dev/sdb`（29.8G Storage_Device，`lsblk` 确认，以实际为准）；分区只需做一次（清场 `sgdisk -Z` + 分区 `sgdisk -g` 建 5 区 fsbl1/fsbl2/ssbl/bootfs/rootfs），之后每次改 U-Boot 只需 dd 三个镜像到 sdb1/sdb2/sdb3。

### 实验进度（2026-09-19）

- 已完成：实验一 Slide 28-31（安装 SDK 工具链 + 辅助工具）；实验二 Slide 32-34（解压源码 + git am 6 个 ST 补丁）；实验三 Slide 35-42（basic 版配置 + 设备树 + 首次编译）。
- 已完成（2026-09-18）：实验四 Slide 43-49（SD 卡分区烧写 + 首次启动）——指导 `01_移植U-Boot/05_实验四_SD卡分区烧写与首次启动.md` 步骤 1~8 全部回填实测与截图（gdisk 已装、SD 卡锁定 `/dev/sdb`；传承卡旧 GPT 用 `parted mklabel msdos` 擦不掉，改用 `sgdisk -Z` 真正清场（输出 `GPT data structures destroyed!`，partprobe 后 sdb1~sdb5 消失）→ `sgdisk` 重建 5 分区成功（新 GUID `69CB63CB-…`）→ 三条 dd 已写入 sdb1/sdb2/sdb3 → MobaXterm COM11 抓到 SPL 日志 + 复位循环动图）。实测串口每轮输出：`U-Boot SPL 2020.01-stm32mp-r1-g3f0216e7-dirty (Sep 15 2026 - 23:15:55 +0800)` → `Model: …STM32MP157A-DK1 Discovery Board` → `stpmic1_read: failed to read register x : 32board_init_f: probe failed clk=0 reset=0 pinctrl=0 power=-110` → `RAM: DDR3-DDR3L 16bits 533000Khz` → `stpmic1_read: failed to read register x : 39ddr power init failed` → 空行 → `resetting ...`，无限循环 = 预期"电源报错 + 复位循环"（`32`/`39` 与后句挤在一行是本版 SPL 原样输出，不是转写错）。
- 已完成（2026-09-18）：实验五 Slide 50-59（F-1 电源设备树修复）——指导 `01_移植U-Boot/06_实验五_F-1修改设备树电源配置.md` 步骤 2~10 全部回填实测与 11 张截图（六条源码自检实测、编译 + 三条 dd、新串口日志、`[WORKING 2224655f] 5 files changed, 2609 insertions(+)`）。配方：删 `&i2c4`（含 PMIC/type-C 整段）/`&cpu0`+`&cpu1`/`&usbotg_hs` 的 `port` 段/`-u-boot.dtsi` 的 `&pmic`，加 6 个 `regulator-fixed`（v3v3 / v1v8_audio / v3v3_hdmi / v1v2_hdmi / vdd / vdd_usb，`vin` 原有勿重加）、menuconfig 关 CONFIG_PMIC_STPMIC1、`cp .config configs/stm32mp15_fsmp1a_basic_defconfig` + git 提交。实测新串口：电源报错消失 → `Trying to boot from MMC1` → `MMC: no card present` → `spl: mmc init failed with error: -123` → `### ERROR ### Please RESET the board ###`（查过 `lib/hang.c`：hang() 是 `for (;;) ;` 原地死循环、不自复位，串口的两轮来自手动复位或 32s 看门狗）。
- 已完成（2026-09-18）：实验六 Slide 60-65（F-2 SD 卡 CD 引脚）——指导 `01_移植U-Boot/07_实验六_F-2修改SD卡驱动.md` 步骤 1~6 已回填实测与 4 张截图（assets 01~03 为课件原理图/课件截图，04~07 为用户实测：04 改后 nano、05 编译+三条 dd、06 串口动图、07 串口完整日志）；**步骤 7 的 git 提交待用户执行**（全文唯一一处"待补充"）。第一次执行踩坑见下方踩坑记录（`gpiob 3` 漏改 bank 名）；改正后实测串口（16:22 构建，SPL 版本串 `g2224655f-dirty`）：`Trying to boot from MMC1` 后不再报 `-123`，直接出 U-Boot 本体横幅 → `Board: stm32mp1 in basic mode (st,stm32mp157a-dk1)` → `Loading Environment from MMC... OK`（读到的是出厂系统遗留在卡数据区的有效环境，不是 bad CRC）→ `stm32 vrefbuf timed out: -110` 与 `adc@0: can't enable vdd-supply!board_check_usb_power: single shot failed for adc@0[18]!`（挤一行，F-3 对象）→ `Net: eth0: ethernet@5800a000` → 倒计时归零后 autoboot 走网络引导：`EQOS_DMA_MODE_SWR stuckFAILED: -110`×2 → `Wrong Image Format for bootm command`/`ERROR: can't get kernel image!` → 停在 `STM32MP>`。**basic 版 U-Boot 首次完整启动达成**。
- 已完成（2026-09-18）：实验七 Slide 66-68（F-3 去掉 ADC）——指导 `01_移植U-Boot/08_实验七_F-3去掉ADC功能.md` 步骤 1~6 已回填实测与 4 张截图（05 menuconfig 后自检 not set 恰两行、06 编译收尾+lsblk+三条 dd、07 串口动图、08 完整日志；assets 02/03 两张课件图内容与文件名原本对调——02 实为 menuconfig 界面、03 实为两条路径文字总览——已换正并补图注）。实测串口（17:04 构建，版本串 `g8de188df` **无 `-dirty`** = `.config` 不入库、改配置不弄脏工作区，哈希即 F-2 提交 → 实验六步骤 7 已完成）：三条 ADC 报错（含 `vrefbuf timed out`）全部消失、`Err: serial` 直达 `Net:`，EQOS×2 与 bootm 报错照旧，停在 `STM32MP>`。步骤 7（defconfig 同步 + git 提交）**已确认完成**——实验八串口版本串哈希 `g1ac3a506` 即 F-3 提交（哈希接力 `g8de188df`→`g1ac3a506`），已在指导 08 步骤 7 补记（版本串接力：实验四 `g3f0216e7` 初始 → 实验六 `g2224655f` F-1 → 实验七 `g8de188df` F-2 → 实验八 `g1ac3a506` F-3）。
- 已完成（2026-09-18）：实验八 Slide 69（F-4 关闭 LTDC）——指导 `01_移植U-Boot/09_实验八_F-4关闭LTDC显示.md` 步骤 3~6 已回填实测与 6 张截图（02 nano 保存、03 自检实测：改前 347 行 `okay`/改后 `disabled`/`ltdc_ep0_out` 计数 2、04 编译收尾+lsblk、05 三条 dd（98921/98921/853450 字节）、06 串口完整日志、07 串口动图）。配方兑现：`&ltdc` 只改 `status = "disabled"` 一行、`port` 段保留（编译通过即 dtc 检验通过）。实测串口（22:42 构建，版本串 `g1ac3a506-dirty`：哈希 = F-3 提交、`-dirty` = F-4 未提交改动）与实验七**逐行一致**——无新报错、`Err: serial` 直达 `Net:`、EQOS×2 与 bootm 照旧、停 `STM32MP>`，F-4 预期的"安静"里程碑达成。**步骤 7（git 提交）已完成（2026-09-18）**：`[WORKING ec8c29dd] 1 file changed, 1 insertion(+), 1 deletion(-)`，版本串接力至此为 g3f0216e7→g2224655f→g8de188df→g1ac3a506→ec8c29dd（指导 09 步骤 7 已回填）。assets 里 `image-20260918224833102.png` 与 04 md5 相同（重复粘贴的同一张编译+lsblk 截图），未引用备查。
- 实验九已完成（2026-09-18/19）：`01_移植U-Boot/10_实验九_F-5替换网卡驱动.md`（Slide 70-74 → 步骤 1~12；assets 01~06 = slide071_59/60、072_61、073_62/63/64，07~10 = 用户实测：menuconfig 勾选、编译收尾+三条 dd、串口完整日志、串口动图）。**步骤 1~12 全部回填实测与截图**（步骤 4~6 自检：phy.h 395 行声明、Makefile 34 行编译项、Kconfig 187~189 行三行配置项）。rar 解压会套同名子目录 `u-boot-网卡-MAE0621A驱动/`，指导步骤 2 已补"复制到共享文件夹哪一层 + 路径因人而异以终端提示符为准"说明，cp 命令按带子目录的实际路径书写。**F-5 里程碑第一半达成（2026-09-19 00:07 构建，版本串 `gec8c29dd-dirty` = F-4 提交 + F-5 未提交，接力预测兑现）**：`EQOS_DMA_MODE_SWR stuck` 消失，autoboot 走到 `Waiting for PHY auto negotiation to complete`（未插网线每秒一个点地等）；用户 Ctrl+C 中断 → `user interrupt!`/`No link`/`FAILED: 0` → bootcmd 继续两次 TFTP（遗留出厂环境 `serverip 192.168.202.6`/`ipaddr 192.168.202.12`，下载 uImage/dtb）→ `Abort`×2 → bootm 找不到内核照旧 → 停 `STM32MP>`——Ctrl+C 的回显全部正常，日志逐行解读已写入指导步骤 10。**步骤 11 已完成（2026-09-19）——F-5 里程碑全部达成**：`env default -a` 清出厂遗留 → 重新上电、倒计时内按键拦停 → 四条 setenv + `saveenv`×2（`Writing to MMC(0)` 主副本 + `Writing to redundant MMC(0)` 冗余副本）→ `ping 192.168.0.100` → `Waiting for PHY auto negotiation to complete..... done` + `host 192.168.0.100 is alive`。清环境后首次上电报 `invalid MAC address in OTP 00:00:00:00:00:00` + `Error: ethernet@5800a000 address not set.`（环境无 ethaddr 的预期报错，设完即消）；重启后 `Net:` 干净、autoboot 变默认 `Boot over mmc0!`（出厂 TFTP 引导被清掉，`** Unrecognized filesystem type **` 落回命令行）——bootcmd 不再碰网卡，PHY 长串点等待从此不出现；拦停关键一手 = 看准 `Hit any key to stop autoboot` 倒计时按 Enter（已写入指导步骤 11 板子侧开头）。assets 新增 `11_PC网卡IP设置.png`（Windows 侧 USB 网卡 ASIX"以太网 7"手动配 192.168.0.100/255.255.255.0）、`12_ping通PC.png`、`13_重启后环境生效.png`（已按规范命名并补图注；用户贴在步骤 10 结尾的 PC 配置图已挪进步骤 11 PC 侧段落）。步骤 11 注意点区已按用户要求精简：只留"遗留环境怎么清除重设（env default -a）+ 终端粘贴用右键"两条，printenv 缺失/出厂同款 MAC/不可见字符三条详细笔记撤下——`type: m` 报错最终复盘为用户手打敲错，非粘贴污染。配方：资料 rar 三件套——`phy.c`（**覆盖** `drivers/net/phy/phy.c`，厂商改造版，实测 482 行 `phy_init()` 内调用 `phy_maxio_init()`）、`maxio.c`（新增）、`dwc_eth_qos.c`（**覆盖** `drivers/net/` 下同名 MAC 驱动，即 EQOS 报错出处）；`include/phy.h` 在 `phy_realtek_init` 声明后加 `int phy_maxio_init(void);`；`drivers/net/phy/Makefile` 加 `obj-$(CONFIG_PHY_MAXIO) += maxio.o`；Kconfig 加 `config PHY_MAXIO / bool "supports the Maxio MAEXXXX PHY" / default n`（REALTEK 段附近）；menuconfig：Device Drivers → Ethernet PHY (physical media interface) support → `[*] supports the Maxio MAEXXXX PHY`。验证 = 串口不再报 `EQOS_DMA_MODE_SWR stuck`（已达成）+ `setenv ethaddr 1A:1F:DB:0E:69:FD / ipaddr 192.168.0.8 / netmask 255.255.255.0 / saveenv` 后 `ping` 通 PC（PC 网卡手动 192.168.0.X，Windows 防火墙拦 ICMP 的提醒已写入）。**步骤 12 已完成（2026-09-19）——实验九收官**：`git status --short` 恰 7 行（6 M + maxio.c ??）→ `[WORKING dd36022e] 7 files changed, 161 insertions(+), 21 deletions(-)` + `create mode 100755 drivers/net/phy/maxio.c`；版本串接力 = g3f0216e7→g2224655f→g8de188df→g1ac3a506→ec8c29dd→dd36022e，实验十构建从 `gdd36022e` 起跳；161+/21- 与 u-boot.img 仅 +600 字节互证 = 厂商改动是小幅针对性适配（maxio.c 百余行 + 覆盖文件少量修改），非整文件重写。
- 实验十指导已写好（2026-09-18）：`01_移植U-Boot/11_实验十_F-6支持eMMC.md`（Slide 75-81 → 步骤 1~8，全部待补充；assets 01~06 = slide076_65/077_66/078_67/079_68/079_69/080_70）。配方：`fsmp1x.dtsi` 在 `&sdmmc1` 节点**之后**新增 `&sdmmc2` 节点（课件 10 项属性全文已录入指导；`vmmc=<&v3v3>`/`vqmmc=<&vdd>` 即 F-1 固定电源兑现；文件里原本无任何 sdmmc2，纯新增——dkx.dtsi 只有 pinctrl 定义，见踩坑记录）；`stm32mp157a-fsmp1a-u-boot.dtsi` 两处：aliases 加 `mmc1 = &sdmmc2;`（原 aliases 有 i2c3/mmc0/usb0），新增 `&sdmmc2 { u-boot,dm-spl; };`（`&sdmmc1` 的 dm-spl 是 DK1 原有，勿动）。里程碑 = `MMC: STM32 SD/MMC: 0  STM32 SD/MMC: 1`，可选 `mmc dev 1` + `mmc info` 摸底。提交预期 2 个文件。
- 实验十一指导已写好（2026-09-18）：`01_移植U-Boot/12_实验十一_trusted版U-Boot移植.md`（Slide 82-87 → 步骤 1~8，全部待补充；assets 01~02 = slide084_71/086_72）。配方：`cp .config configs/stm32mp15_fsmp1a_basic_defconfig` → `make distclean` → `cp configs/stm32mp15_trusted_defconfig configs/stm32mp15_fsmp1a_trusted_defconfig`（一条命令）→ `make stm32mp15_fsmp1a_trusted_defconfig` → menuconfig **重做三站四处**（F-1 关 PMIC_STPMIC1：Device Drivers → Power；F-3 关 CMD_ADC + ADC；F-5 开 PHY_MAXIO；F-2/F-4/F-6 是设备树天然继承不用重做）→ `cp .config configs/stm32mp15_fsmp1a_trusted_defconfig` → `make -j2 all DEVICE_TREE=stm32mp157a-fsmp1a` 产 `u-boot.stm32`（trusted 无 SPL，原因见踩坑记录）→ 出厂 `tf-a-stm32mp157a-fsmp1a-trusted.stm32` dd 到 sdb1+sdb2、`u-boot.stm32` dd 到 sdb3。里程碑 = TF-A 日志开场（无 SPL 横幅）+ `Board: stm32mp1 in trusted mode (st,stm32mp157a-dk1)` + `MMC: … 0, … 1`。提交预期 1 个文件；结尾引 Slide 87 指向第 4 章《使用U-Boot》。
- 当前进度小结：指导文档 01~12 已全部写齐；实验一~九全部完成（F-1~F-5 全部提交，U-Boot 工作区干净，板子 ping 通 PC）；下一个 = 实验十（F-6 eMMC，指导 `11_实验十_F-6支持eMMC.md` 已写好、全部待补充），之后实验十一（trusted 版）收尾第 3 章。
- 后续实验材料在 `D:\桌面文件\资料\嵌入式linux`：`tf-a-stm32mp157a-fsmp1a-trusted.stm32`（trusted 版 TF-A）、`u-boot-网卡-MAE0621A驱动.rar`（F-5 的 phy.c/maxio.c/dwc_eth_qos.c）、`u-boot电源配置-设备树.dts`（F-1 参考）、`官方系统内核和设备树.zip`、kernel 相关 zip、buildroot/busybox 等。

### 实验踩坑记录（避免重复踩）

- SDK 解压目录里**没有 `install.sh`**，安装脚本是长名字的 `st-image-weston-*.sh`；安装目标目录必须直接回车用默认 `/opt/st/...`（虚拟机共享文件夹不支持 Linux 符号链接，工具链不能装在里面）。
- `git init` 必须在解压出的 `u-boot-stm32mp-2020.01` 子目录里执行；在外层 `r0` 目录执行会把补丁文件本身当源码提交，且 `../*.patch` 找不到补丁（`rm -rf .git` 后重来）。
- `cc1: error: bad value ('generic-armv7-a') for '-mtune=' switch` = 当前终端没激活工具链，make 落到了 x86 宿主机 gcc（报错列出的 -mtune 参数全是 Intel/AMD CPU 即铁证）→ `source` 工具链后续编即可，无需 clean。
- `fatal: 已经有一个 gc 正运行`、ST 补丁的空白字符警告（`new blank line at EOF`）均无害，忽略。
- 编译在共享文件夹里能跑但慢，Ctrl+C 后 `make -j2` 可安全续编（进度保留）；若报符号链接/权限类错误，把源码移到虚拟机本地磁盘（如 `~/FS-MP1A/`）再编（挪动后需 `make distclean` + 重新 defconfig）。
- `sudo parted -s /dev/sdb mklabel msdos` **无任何输出是正常的**（`-s` 脚本模式成功即静默、瞬间完成，不用等），但它**不会清掉旧 GPT**：实测复查 `sgdisk -p` 连磁盘 GUID 都不变（sgdisk 见 GPT 有效就继续按 GPT 显示）。要真清场用 `sudo sgdisk -Z /dev/sdb`（GPT、MBR 数据结构一并摧毁，官方定位就是"重新分区之前"用它），再 `sudo partprobe /dev/sdb` 刷新——`lsblk`/`/dev/sdbN` 只是内核缓存的旧分区表，不 partprobe 就永远显示旧分区。实测顺序：**先 partprobe 没用**（旧 GPT 还在，重读出来还是旧分区），必须 `-Z` 之后再 partprobe 才看到分区节点消失；`-Z` 的成功输出是 `GPT data structures destroyed!`。空盘上 `sgdisk -p` 显示 `Creating new GPT entries in memory.` + 一个随机 GUID（内存态，写盘才固定）。
- `sgdisk -g` = `--mbrtogpt`（把 MBR 标签的盘转成 GPT），不是"使用 GPT 分区表"；在 MBR/BSD 标签的盘上写入必须带它，否则 sgdisk 拒绝写盘（退出码 3）。
- **写 F 系列指导时的"标准答案"来源**：DK1 的 `stm32mp15xx-dkx.dtsi`（= 用户文件 `stm32mp15xx-fsmp1x.dtsi` 的原样拷贝，行号完全一致）可从 `D:\桌面文件\资料\嵌入式linux\en.SOURCES-*.tar.xz` 追出——外层 xz 里是 `u-boot-stm32mp-2020.01-r0.tar.gz`（内层不含 dkx.dtsi）+ 6 个补丁；dkx.dtsi 是补丁 0004（DEVICETREE）**新建**的文件，取该补丁 `new file mode` 段剥掉行首 `+` 即得全文（同理可解出任意 ST 源码文件，写后续指导前先核对实际内容再下笔）。
- **F-1 文件锚点**（`stm32mp15xx-fsmp1x.dtsi`）：`/ {` 根节点 11~98 行，末尾**已有** `vin`（91~97），6 个固定电源追加在 vin 之后；`&cpu0`/`&cpu1` 在 134~140；`&i2c4` 整段 261~443（含 `stusb1600@28` 与 `pmic: stpmic@33`）；`&usbotg_hs` 在 736~747（只删 `port { ... };` 段，phys/phy-names/usb-role-switch/status 保留）。删 &i2c4 后仍被引用的电源标签即固定电源清单的由来：vdd（&adc/&vrefbuf）、v3v3（&sdmmc1/2 的 vmmc-supply）、v1v8_audio（音频）、v3v3_hdmi/v1v2_hdmi（HDMI）、vdd_usb（&usbphyc_port0/1 的 phy-supply）。
- **F-2 一行改动**（`stm32mp15xx-fsmp1x.dtsi` 的 `&sdmmc1`）：`cd-gpios` 由 `<&gpiob 7 (GPIO_ACTIVE_LOW | GPIO_PULL_UP)>` 改为 `<&gpioh 3 ...>`（DK1 原始为 dkx.dtsi 第 575 行；F-1 增删后约在 440 行附近）。**不需要额外使能 gpioh**：板级 `stm32mp15xxac-pinctrl.dtsi`（被 fsmp1a.dts 包含）里 gpioh 已是 `status = "okay"`，`stm32mp15-u-boot.dtsi` 里又标了 `u-boot,dm-pre-reloc`（SPL 可用）。`-123` = `-ENOMEDIUM`。厂商侧交叉验证：`资料/嵌入式linux/kernel-初始设备树.zip` 里 FS-MP1A 内核 dtsi 的 `&sdmmc1` 与课件一致（同 zip 还有 `官方系统内核和设备树.zip` 的 `stm32mp157a-fsmp1a-mipi050.dtb`）。
- **F-2 踩坑实录（2026-09-18 第一次执行）**：把 `gpiob 7` 只改了数字 → `gpiob 3`，**漏改 bank 名**，编译烧写三条 dd 全部成功但串口原样报 `-123`。排查钥匙是 SPL 版本串：`g2224655f-dirty (Sep 18 2026 - 15:19:33)` 与上次（`g3f0216e7 … Sep 15`）哈希、时间戳都变了 = 卡上镜像确实换过 → 问题只剩"源码那行没改对"，回看 nano 截图立刻定位。教训：GPIO 要改的是"**组名 + 序号**"两个词；步骤里的 `grep -n "cd-gpios"` 自检不能跳。用户 assets 里留有这次失败的 3 张截图（`image-20260918151853302/152349286.png`、`QQ20260918-152446-HD.gif`，未被文档引用），按"走弯路不入正文"惯例保留在 assets 备查、不入库引用。
- **F-4 锚点与"别删 port"**（`stm32mp15xx-fsmp1x.dtsi` 的 `&ltdc`）：DK1 原始节点 = 3 行 pinctrl + `status = "okay"` + `port { ltdc_ep0_out: endpoint@0 { remote-endpoint = <&sii9022_in>; }; }`（patch 0004 核对过）；sii9022 与 cs42l51 同在 **&i2c1** 下（非 &i2c4，故 F-1 删 i2c4 未伤及，编译才一直通过），其 `sii9022_in` **反向引用** `&ltdc_ep0_out` → F-4 只能改 `status` 一行、删 `port` 必报 `Reference to non-existent node or label`。课件 slide069 截图是节点上半截（连收尾 `};` 都没拍到），"截图里没有 port"≠"要删 port"。另：`stm32mp15-u-boot.dtsi` 里有 `&ltdc { u-boot,dm-pre-proper; }` 叠加，与 status 改动不冲突。
- **basic 版 U-Boot 命令集是裁剪过的**（2026-09-19 实测）：`printenv` 报 `Unknown command`（未编入），查变量用 `env print` 替代；课件流程只用 setenv/saveenv/ping 故不受影响。拦停 autoboot 的正确姿势 = **倒计时内按任意键（回车）**直接进 `STM32MP>`（全程不碰网络）；倒计时归零后 autoboot 会拉网卡——`Waiting for PHY auto negotiation` 逐秒打点，Ctrl+C 可掐（`user interrupt!`/`No link`/`FAILED: 0`/`Abort` 均为正常回显），但该会话已"用过网络"，ethaddr 要断电重启后再设。**write-once 与 ethaddr（2026-09-19 定论）**：出厂遗留环境带整套网络配置（ethaddr=1A:1F:DB:0E:69:FD=课件 MAC、serverip 192.168.202.6 等），ethaddr 内置 `mo` 双标志（本构建未开 ENV_OVERWRITE/OVERWRITE_ETHADDR_ONCE：m=MAC 格式校验、o=write-once 已有值不许覆盖）→ 直接重设会被拒；解法 `env default -a` 清整套遗留（ethaddr 一并清空）→ 四条重设 + saveenv（可敲两次把主/冗余两份副本都刷了）。过程中出现过的 `## Error: flags type check failure ... (type: m)` 报错最终复盘 = 用户手打敲错（重打即过），不是格式校验问题（源码 `env/flags.c` 的 `eth_validate_ethaddr_str` 用 isxdigit 判定，课件 MAC 本身合法；write-once 的拒绝报错文案是 `Can't overwrite`）。VM/MobaXterm 终端里粘贴命令用**右键**（Ctrl+C 是中断、Ctrl+V 不生效）。
- **F-5/F-6/trusted 锚点（patch 0004 + 课件逐字核对过）**：F-5 的 `phy.c`/`dwc_eth_qos.c` 是**覆盖**同名文件的厂商改造版（新 phy.c 的 `phy_init()` 会调用 `phy_maxio_init()`，可 grep 验证拷贝成功），`maxio.c` 纯新增；F-5 已提交兑现（`dd36022e` = 恰 7 文件，161+/21-）。F-6 纯新增：dkx.dtsi **没有** `&sdmmc2` 板级节点（全 patch 只有一处 `+&sdmmc2` 在 ev1 系文件里；dkx 只有 pinctrl 五个定义 + `stm32mp15-u-boot.dtsi` 的 compatible 覆盖），官方参照是 `stm32mp157a-ed1-u-boot.dtsi`（EV1 板自带 eMMC：mmc1 alias + `&sdmmc2 { u-boot,dm-spl; }` + sdmmc2 两个 pinctrl 节点的 dm-spl——课件只要求控制器节点，pinctrl dm-spl 未要求）；`&sdmmc1 { u-boot,dm-spl; }` 是 DK1 原有。aliases 里 `i2c3 = &i2c4` 在 F-1 删板级 &i2c4 后仍合法：控制器本体 `i2c4: i2c@5c002000` 在 SoC 级 `stm32mp151.dtsi`（status 默认 disabled），F-1 删的只是板级扩展段。trusted 版：dk1-u-boot.dtsi（= 用户 `-u-boot.dtsi`）里 clk_hse/pmic/rcc 时钟树/sai2/sdmmc1 dm-spl 等整段包在 `#ifndef CONFIG_STM32MP1_TRUSTED`——trusted 编译自动剔除（电源时钟归 TF-A 经 SCMI 管控；`stm32mp15-u-boot.dtsi` 同样 `#ifndef TRUSTED` 才 include no-scmi），trusted 配置下无 SPL、烧写组合 = TF-A×2 + `u-boot.stm32`。课件 trusted 配置起点 = `stm32mp15_trusted_defconfig`。
