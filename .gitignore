# 原始课件（体积大，内容已完整转化为 md）
*.dps
*.pptx

# 课程配套的原始 word 资料（内容已转为 01_移植U-Boot/附录_开发环境搭建_Linux系统安装.md）
*.docx

# 大体积安装包/镜像
*.zip
*.tar.xz

# 解压出的 SDK 大目录（01_移植U-Boot/ 内）
en.SDK-*/

# 固件二进制素材（TF-A/U-Boot 镜像，正文引用、不入库；git 换行规范化会改坏二进制）
*.stm32
